# 프로젝트 기록 (누적 — 필요한 커맨드만 read)

> `PROJECT_STATE.md`의 참조용 짝. **매 단계 읽지 않는다.** 아래 「읽는 시점」에 해당할 때만 읽는다.
> 이 파일은 길어져도 된다. 대신 `PROJECT_STATE.md`를 짧게 유지한다.

## 읽는 시점

| 절 | 읽는 커맨드 |
|---|---|
| 피드백 대장 | `/verify`, `/handoff`, 피드백 번호를 부여·조회할 때 |
| 요구사항 변경 이력 | `/verify`, `/handoff` |
| 구현 상태 · 테스트 증거 | `/verify`, `/handoff` |
| 검증 결과 | `/verify`(write), `/handoff` |
| 명세 개정 이력 | `/design` §5(write), `/verify`, `/handoff` |
| 폐기한 접근 | 같은 논점이 다시 올라올 때 |
| 포트폴리오 자동화 상태 | `/handoff`(모든 유형) |
| 인사이트 수집 | `/insight`(read·add·promote) |

## 피드백 대장 (셀 상태 — 번호는 재사용·재정렬 금지)
> 출처와 무관하게 번호를 이어 붙인다. **코드에는 번호를 심지 않는다.**
> 지적과 코드의 대응은 이 표가 `커밋 해시`·`파일:라인`으로 갖는다(게이트 `L2`가 코드 내 태그를 BLOCK한다).
> **조치가 끝나도 행을 지우지 않는다.** 미조치 건만 `PROJECT_STATE.md`에 요약해 둔다.

| N | 출처 | 지적 | 조치 | 커밋 해시 | 파일:라인 | 회귀 테스트 |
|---|------|------|------|-----------|-----------|-------------|
| 1 | 본인 발견 | `[디벨롭: X]` 내부 태그가 공개 코드에 잔존. `d2f8a0d 공개 전 식별정보 제거`를 거쳤는데도 남았다 | **해소(2회에 걸침).** 1차로 `loan_agent/*.py` 29곳을 제거했으나, **검색 범위를 `loan_agent/`로만 잡아 「전부 제거」라고 보고한 것이 틀렸다.** `/build` 중 저장소 전체를 다시 훑어 `pyproject.toml`·`tests/test_products.py`·`.github/workflows/ci.yml`·`loan_agent_demo.ipynb` **4곳을 추가 제거**했다. 태그만 없애고 설명 문장은 유지 | 미커밋 | `loan_agent/*.py` · `pyproject.toml` · `tests/test_products.py` · `.github/workflows/ci.yml` · `loan_agent_demo.ipynb` | `grep -rn "\[디벨롭" .` → 0건 · `pytest` |
| 2 | 게이트 `X1` | `loan_agent/app.py:649` 하드코딩 자격증명 | **게이트 결함으로 확인·수정 완료.** 오탐이 아니라 게이트 버그였다 — v9.16이 추가한 따옴표 없는 대입 패턴(`ASSIGN_SECRET_BARE`)이 설정 파일용인데 소스 코드에까지 적용돼, 파이썬의 `api_key = _effective_api_key()` 같은 평범한 대입이 전부 걸렸다. **대상을 설정·환경 파일 확장자로 한정**했고 프로젝트 사본과 `~/.harness-src` 양쪽에 적용했다 | `aaacf11` | `tools/gate.py` | `test_gate.py` 「X1 반증: 소스의 함수 대입」 |
| 3 | 게이트 `G3` | `docs: 문서 문체를 학술체로 통일` 커밋이 저자 지문을 지웠다(213줄 삭제·232줄 추가, 정보량 동일) | **정정.** 앞서 「문서 개편에서 자연스럽게 해소, 개편 후 `G3` 소멸 확인」이라고 적었으나 **성립하지 않는다** — `G3`는 `git log`의 커밋 제목을 스캔하므로 문서를 다시 써도 과거 커밋(`e2455da`)은 이력에 영구히 남는다. 이력 재작성 없이는 사라지지 않으며, 그 재작성은 `P2`와 같은 이유로 기각했다. **조치: 게이트를 고쳤다(`aaacf11`)** — `--commit` 모드에서 정보로 강등해 커밋을 막지 않되, 일반 게이트에서는 BLOCK을 유지한다. 지적 자체는 유효하기 때문이다. 문서 개편은 별개로 진행하되 그것이 `G3`를 지우지는 않는다 | `aaacf11` | `tools/gate.py` | `test_gate.py` 「커밋: 과거 문체 커밋 비차단」 |
| 4 | 게이트 `I3` | `/health`가 정적 응답 — 의존 자원이 죽어도 healthy | **진짜. 계획이 이미 해소한다.** 세부기술서 §9.4가 `/health/live`·`/health/ready` 분리, §15.3이 readiness에서 DB·마이그레이션 확인. 게이트가 계획의 타당성을 역으로 확인해줬다 | — | `loan_agent/api.py:58` | readiness 통합테스트 |
| 5 | 게이트 `D2` | `README.md`에 한계·미해결 절이 없다 | **진짜. 미조치 — `/design`에서 처리.** 전략기획서 §7 10페이지의 「검증된 범위·한계」·§12 금지 과장과 직결된다 | — | `README.md` | `gate.py` `D2` |
| 6 | 게이트 `P2` | 커밋 author 이메일 `rhkdzhf54@naver.com` 노출 | **범위 축소 확인 후 미조치 결정.** git 설정은 로컬·전역 모두 이미 `59742337+Gwangdev@users.noreply.github.com`이라 **앞으로의 커밋은 전부 noreply로 나간다.** 남은 것은 기존 28개 커밋의 author 값뿐이며, git은 author를 커밋 객체에 저장하므로 설정 변경이 소급되지 않는다. 이력 재작성(`filter-repo --mailmap`)은 해시 28개가 전부 바뀌고 force push가 필요해 되돌릴 수 없다. **실익(스팸 수집 방지)이 대가보다 작고, 「과거를 지우지 않고 앞으로를 바르게 한다」는 `G3` 처리 원칙과 일관되므로 그대로 둔다** | — | git 이력(28커밋, 2026-07-27) | — |
| 7 | 게이트 `S2` | 미호출 공개 함수 3건 | **전부 설명 가능. 미조치.** `run_logic_selftest()`=노트북·문서에서 호출하는 공개 API / `tool()`=crewai 부재 시 no-op 데코레이터 폴백(데코레이터로 실제 사용) / `worker()`=`threading.Thread(target=worker)`로 호출. 축 7은 정적 참조만 세므로 WARN | — | `loan_agent/core.py`·`app.py` | — |
| 8 | 게이트 `C1` | 인라인 주석 중 '왜'형 52/158 = 33% (목표 40%) | **미조치 — 관찰 중.** 하네스 README가 임계값을 잠정이라 명시(3~4회 사용 후 조정). 재작성 이후 재측정한다 | — | `loan_agent/` | — |
| 9 | 설계 검토 §6 | **T1 #6이 LLM 재시도를 직접 구현하겠다고 적혀 있었다.** 검토 기준(「재시도·서킷브레이커를 직접 구현하지 않았는가」)과 `CLAUDE.md`(「프레임워크 제공 기능 기본 채택」)를 동시에 위반 | **해소.** SDK 내장 재시도 채택 + 횟수·대기 상한 명시로 정정(ADR-018). **내가 만든 결함이며, 1차 검토(요청한 것이 동작하는가)로는 보이지 않고 2차 검토(관례 대비)에서만 나왔다** | — | `docs/설계결정.md` ADR-018 | 재시도 상한 통합 테스트 |
| 10 | 설계 검토 §4 | **ADR-013이 `current_explanation_run_id`를 도입하며 불변 심사 레코드에 유일한 가변 필드를 만들었는데, 갱신 경쟁을 다루지 않았다.** 재시도 두 건이 동시에 완료되면 lost update | **해소.** `PENDING`·`RUNNING` 실행이 있으면 409, 부분 UNIQUE 인덱스로 DB 강제, 갱신 시 심사 행 잠금(ADR-019) | — | `docs/설계결정.md` ADR-019 | 병렬 재시도 요청 → 1건만 수용 |
| 11 | 설계 검토 §3·§5 | 오류 정책이 불완전 — 빈 컬렉션 처리·오류 본문 형식·목록 limit 상한·필터 화이트리스트 미정 | **해소.** `SPEC.yaml`에 명시 — 빈 결과는 200+빈 배열(404·204 아님), RFC 9457 `problem+json` 전 구간 통일, limit 기본 20·상한 100을 **서버가 강제**, status·기간 필터를 허용목록 대조하고 컬럼명·ORDER BY로 흘리지 않음 | — | `SPEC.yaml` | 목록 API 통합 테스트 |
| 12 | 설계 검토 §1 | `GET /api/v1/products`의 요구사항(R8)이 **이번 설계에서 새로 만들어졌다.** 기존 `/products`를 살리려는 사후 정당화일 가능성 | **해소 — 제거(2026-08-26 사용자 승인).** R8은 사용자 시나리오가 아니라 **기존 엔드포인트에서 역산**됐고 그것이 사후 정당화의 표식이다. 감사는 결과에 저장된 `product_dataset_version`으로 충족되고, 상품 단건 조회는 LLM 도구 경로에만 필요하며 이는 HTTP 표면이 아니다. **R8 번호는 공백으로 남겨 제거 사실이 보이게 했다.** 필요해지면 §5 명세 개정으로 사유와 함께 재등재 | — | `SPEC.yaml` | — |

출처 예: `사용자` · `본인 발견` · `independent-verifier` · `외부 도구 리뷰` · `타팀 리뷰`.
미채택 지적도 남기고 조치란에 사유를 적는다.

## 승인된 요구사항 · 수용 기준 · 변경 이력

### 관통 논리 (2026-08-26 사용자 확정 — 임의 변경 금지)

> 신뢰할 수 없는 기술을 개선하기보다는, 확실한 결과를 보장받을 수 있도록 **기술 적용 범위를 조절**하고 **신뢰 가능한 다른 기술을 함께 사용**했다.

뺄셈과 덧셈은 쌍이다. "LLM에서 무엇을 뺐다"를 쓸 때는 반드시 "무엇이 그 자리를 맡았다"를 같은 문장에 붙인다. 범위 축소만 말하면 AI 불신으로 읽히고, 채운 기술을 함께 말해야 보장 수준에 따른 기술 배치로 읽힌다.

| 대상 | 개선하지 않은 것 | 적용 범위 조절 | 함께 쓴 신뢰 가능한 기술 |
|---|---|---|---|
| 판정 | LLM 정확도 튜닝 | LLM은 설명만 | `screen_loan` 결정적 함수(CSV 하드규칙) |
| 파싱 | LLM 파싱 신뢰도 | LLM 파싱은 후보까지 | 규칙기반 파서 교차검증 + 필수필드 게이트 + 사용자 확정 폼 |
| 인젝션 | 입력 필터 강화 | 판정을 LLM 밖에 배치 | 도구 강제 — CSV 밖 값 생성 경로 없음 |
| 수치 | 환각 억제 프롬프트 | 수치는 생성 대상이 아님 | ReAct 도구 조회 + Eval 수치근거 지표 |
| 품질 | 프롬프트 튜닝 반복 | 감으로 판단하지 않음 | Eval 하네스 + 결함 주입 역검증 |

### 서술 3단 구조 (법적 공백·기술 공백 공통)

```
① 실무에서 이 조건이면 이것이 적용·필요하다     ← 지식
② 본 데모는 그 조건 밖으로 범위를 설계했다        ← 판단
③ 실서비스로 전환하면 무엇을 검토·도입해야 하는가  ← 확장 이해
```

③이 없으면 "안 걸리게 피했다"로 끝나 소극적으로 읽힌다. 공백을 서술할 때 3단을 모두 채운다.

### 포트폴리오가 반드시 전달해야 할 4개 축 (2026-08-26 사용자 요구)

단순 서브에이전트 과제로 인지되면 안 된다.

1. **멀티에이전트 기술의 본질 이해** — 경계를 만드는 목적은 경계마다 결정적 검증을 삽입할 수 있게 하는 것. 산물은 지능이 아니라 검증 가능성
2. **금융 도메인 × 기술 결합 인사이트** — ①앙상블은 오류가 독립일 때만 안전(단일 모델 오류는 상관관계를 가져 SC 다수결이 틀린 답에 수렴 가능) ②인젝션 방어를 추가하지 않고 공격 표면을 제거 ③"동작한다"와 "맞다"는 다르다(간이 DTI는 부채 0이면 항상 여유로 나왔다)
3. **기술적 확장 시도** — Eval의 자랑거리는 30/30 통과가 아니라 **결함 7종 주입으로 평가기 자체를 검증**한 것
4. **리스크 → 안전장치 → 근거의 합당성** — 대안 비교 없는 선택은 근거가 아니다. ADR·리스크 대장에 **기각한 대안과 사유**를 필수 칸으로 둔다

### 기술 공백 처리 등급 (T1/T2/T3)

전부 구현할 수 없으므로 위 3단 구조로 나눈다. **T2는 "안 했다"가 아니라 "이 조건이면 필요해지고 그때 이렇게 한다"로 답하면 T1과 같은 무게를 갖는다.**

**T1 — 구현해 증거로 남긴다 (11건, 2026-08-26 승인)**

| # | 항목 | 근거 |
|---|---|---|
| 1 | docker-compose 네트워크 격리 | 게이트 축 I(`I1`·`I2`·`I5`)가 기계 검증 → 3-tier 경계가 증거로 남는다 |
| 2 | DB 커넥션 풀 설정 + 산정 근거 | **ADR-022에서 확정.** 커넥션은 동시 사용자 수가 아니라 **점유 시간**으로 정해진다(Little's Law). LLM을 트랜잭션 밖에 두면 100 req/s에 필요 커넥션 **1개**, 안에 두면 **약 2,000개** — `max_connections` 기본값 100의 20배라 어떤 풀 크기로도 못 버틴다. **이 대비가 ADR-003의 값어치다** |
| 3 | 커서 기반 페이지네이션 | OFFSET은 깊이에 따라 열화 |
| 4 | 복합 인덱스 + `EXPLAIN ANALYZE` 전후 | 인덱스를 나중에 추가하는 마이그레이션으로 분리해야 전후 비교가 성립(D5) |
| 5 | 타임아웃 계층 | 클라이언트 > 게이트웨이 > 앱 > DB 순으로 짧아져야 좀비 요청이 안 쌓인다 |
| 6 | LLM 재시도 — **SDK 내장 채택 + 횟수·대기 상한 명시** | **설계 검토 §6에서 정정(ADR-018).** 처음엔 「백오프 직접 구현」으로 적었으나, 직접 구현은 호출 체인의 계층마다 재시도가 겹쳐 지연이 곱해진다. 프레임워크 제공 기능 기본 채택 규칙에도 어긋났다 |
| 7 | 아웃박스 패턴 명시 | `explanation_run(PENDING)`을 같은 트랜잭션에 넣는 것이 사실상 outbox — 명시하면 강해진다 |
| 8 | 부하 테스트 1회 | 안 하면 "충분한가"에 답할 실증 근거가 없다 |
| 9 | **동시성 테스트** | 같은 Idempotency Key로 병렬 요청 N개 → 심사 1건만 생성되는지. **없으면 P0 핵심 주장이 무증거로 남는다** |
| 10 | 토큰 비용 모델링 | `usage_metrics`를 이미 수집 중(A7 미노출). 화면 노출 + README 표 |
| 11 | UUID PK 선택 근거 문서화 | UUIDv4는 B-tree 삽입 시 페이지 분할이 잦아 인덱스 팽창. 알고도 데모 규모라 채택했다고 쓰면 그 자체가 증거 |

**T2 — 판단으로 방어** 큐·워커 분리 / Redis 공유 rate limit(현재 `session_state` 기반이라 다중 인스턴스에서 무력화) / 파티셔닝·아카이빙 / 무중단 마이그레이션(expand-contract) / JWT·세션·WAF / SLO·알림 / 감사 로그 무결성(append-only·해시체인)

**T3 — 명시적 비범위** k8s · 멀티리전 · 서비스메시 · Kafka (세부기술서 §16에 기재됨)

### 측정 계획 — 부하(T1 #8)·토큰 비용(T1 #10)

두 항목은 결정이 아니라 **측정**이므로 ADR이 아니라 수용 기준으로 둔다. 다만 **무엇을 어떤 기준으로 재는지**를 미리 정하지 않으면 사후에 유리한 숫자만 고르게 된다.

#### 부하 테스트

**대상은 결정적 경로(`POST /api/v1/assessments`)뿐이다.** 설명 생성 경로는 LLM 제공자 지연이 지배하므로 **우리 시스템 성능을 측정하지 못한다** — 재면 제공자를 재는 것이다. 이 구분 자체가 방어질문 ①의 답이다.

| 항목 | 내용 |
|---|---|
| 지표 | p50 · p95 · p99 지연, 처리량(req/s), 오류율 |
| 조건 | 동시 10 / 50 / 100, 각 60초 |
| 함께 기록 | 하드웨어, 데이터 건수, 커넥션 풀 설정(ADR-022) |

**합격·불합격 기준을 두지 않는다.** 포트폴리오 규모에서 SLO를 선언하면 근거가 없다. 목적은 **어디서 꺾이는지(포화점)를 찾는 것**이고, 「충분한가」라는 질문에는 **「이 조건에서 여기까지는 견디고 그 이상은 측정하지 않았다」**로 답한다.

ADR-022의 계산(필요 커넥션 ≈ 1개)이 맞는지 여기서 검증된다. **풀이 포화되기 전에 다른 곳이 먼저 꺾이면 계산이 옳았다는 증거다.**

**절대값을 일반화하지 않는다.** 실행 환경과 데이터 건수를 반드시 함께 적는다.

#### 토큰 비용

`usage_metrics`는 이미 수집 중이고 `explanation_run`에 `input_tokens`·`output_tokens` 컬럼이 있다(데이터모델 §2). 남은 것은 노출이다.

| 항목 | 내용 |
|---|---|
| 산출 | 요청당 평균 입력·출력 토큰 → 단가 적용 → 요청당 비용 → 월 N건 추정 |
| 표시 | 화면 + README 표 |
| 단가 취급 | **상수로 분리하고 출처·기준일을 함께 적는다.** 단가는 바뀌므로 본문에 숫자를 박으면 문서가 조용히 틀려진다 |

**3-Agent 구조의 비용을 정직하게 드러내는 것이 목적이다.** 경계 3개는 LLM 3턴이고 ReAct 재조회가 더해진다. 방어질문 ⑳(Agent를 2·4개로 바꾸면?)에 **「경계 1개당 비용이 얼마인지」** 수치로 답할 수 있게 된다.

### P0 내부 우선순위 — D8 해소 (`/design` 2026-08-26)

**문제:** 세부기술서 §20의 P0가 12항목 전부 「축소 불가」로 지정돼 있어, §11 중단·축소 기준이 발동해도 줄일 것이 없었다. 통제 장치가 이름만 있고 작동하지 않는 상태였다.

**해소:** P0를 둘로 나누고, 축소 가능한 쪽에는 **하한**을 함께 적는다. 하한이 없으면 축소가 삭제가 된다.

**P0-a — 양보 불가.** 이것이 빠지면 포트폴리오의 주장 자체가 무너진다.

| # | 항목 | 없으면 무너지는 주장 |
|---|---|---|
| 1 | 결정적 판정과 LLM 설명의 **코드 경계** 분리 | 관통 논리 전체 |
| 2 | PostgreSQL + Alembic 마이그레이션 | 「DB 설계를 할 줄 안다」 |
| 3 | 심사·판정·추천 **단일 트랜잭션** 저장 | 「정합성을 보장한다」 |
| 4 | Idempotency(키+요청해시+UNIQUE) **+ 동시성 테스트** | 「중복 요청을 막았다」 — 테스트가 없으면 무증거(T1 #9) |
| 5 | LLM 실패 격리 — 판정 보존 | 「부분 실패가 오염되지 않는다」 |
| 6 | 규칙·상품·모델·프롬프트 **4종 버전 저장** | 「재현·감사 가능하다」(축 4) |
| 7 | Docker Compose 3-tier + 게이트 축 I | 「3-tier 보안을 안다」(방어질문 ③) |
| 8 | README가 실제 구현과 일치 | 전략기획서 §13 체크리스트 |
| 9 | 60~90초 영상 데모 | §7 — 영상이 **기본 증거**, 라이브는 보조 |

**P0-b — 축소 가능. 하한까지만 줄이고 0으로 만들지 않는다.**

| # | 항목 | 하한(여기까지는 반드시) |
|---|---|---|
| 10 | 감사 이벤트 · Correlation ID | correlation_id 로그 전파 + `audit_event` 액션 3종 |
| 11 | Eval 케이스 확대(10~15건) | 경계 5건 + **결함 주입 7종은 유지**(축 3의 핵심) |
| 12 | API 통합 테스트 | 정상 / 422 / 409 / 조회 4건 |
| 13 | 부하 테스트 | 1회 실행 수치만 있으면 됨(T1 #8) |
| 14 | T1 잔여 8건 | 구현이 어려우면 **문서화만** — 판단으로 방어(T2 방식) |

**축소 발동 순서(§11 트리거 시):** 14 → 13 → 12 → 11 → 10. **P0-a는 어떤 경우에도 축소 대상이 아니며, P0-a가 위태로우면 기능을 줄이는 게 아니라 제출 범위를 줄인다.**

### 방어 질문 33건 (코드리뷰 시점 교차검증용)

> 2026-08-26 확정. **프로젝트 디벨롭 이후 코드리뷰에서 이 목록으로 누락을 교차검증한다**(사용자 판단 — 코드 없이 질문만 늘리면 수확체감).

**A. 동시성·부하** ①동시 트래픽 대비책, 현재로 충분한가(병목은 DB가 아니라 LLM이 요청 수명을 지배 — `/advise`가 3-Agent 전체를 `await`로 10~30초 점유) ②실제 DB 연결 시 난이도 급증(N+1·OFFSET 열화·커넥션 고갈·격리수준·무중단 마이그레이션·이력 무한증가) ③3-tier 보안 고도화(본질은 네트워크 경계로 DB를 외부에서 못 닿게 하는 것. **CORS는 방어가 아니다 — 브라우저만 지키는 규약이라 서버 간 요청엔 없다**)

**B. AI·LLM 운영** ④LLM 429·5xx 재시도 횟수·간격 ⑤타임아웃 계층 ⑥Idempotency Key TTL(없으면 정합성 장치가 용량 문제로 바뀐다) ⑦트랜잭션 커밋과 비동기 발행의 원자성 ⑧인스턴스 2대면 rate limit은 ⑨복합 인덱스 컬럼 순서 근거 ⑩부하 테스트 여부 ⑪장애 시 어디부터 보나(SLO·알림 기준 없음) ⑫감사 로그를 관리자가 지우면 ⑬JSONB vs 정규화 ⑭배포 중 마이그레이션 실패 ⑮모델 교체 시 무엇이 깨지나 ⑯프롬프트 수정 후 Eval 통과 = 배포 가능인가(5케이스 통과는 5케이스 통과일 뿐) ⑰LLM-as-judge를 왜 안 썼나(판정자가 비결정적이면 회귀 판정도 비결정적) ⑱요청당 토큰 비용·월 1만 건 ⑲프롬프트가 길어지면(비용·지연 + lost-in-the-middle) ⑳**Agent를 2·4개로 바꾸면** ← 본질 이해의 최종 판별. "3개가 적당해서"는 실습 과제, "경계마다 검증을 하나씩 넣을 수 있어서"는 설계자

**C. 데이터 모델(SQLD 연결)** ㉑`recommendation`을 왜 별도 테이블로(JSONB엔 UNIQUE 제약을 못 건다) ㉒`decision_result`가 1:1인데 왜 분리(생명주기가 다르다 — 심사는 상태가 변하고 판정은 불변) ㉓정규화를 어디서 멈췄나 ㉔상품 데이터를 왜 CSV에(버전 고정·결정성. 실서비스면 DB로 가야 하고 그때 `product_dataset_version`은) ㉕**UUID PK 성능** ← 강한 카드

**D. 테스트** ㉖테스트 54개가 무엇을 보장하나(개수는 지표가 아니다. 무엇을 안 덮는지 말할 수 있는가) ㉗Testcontainers를 왜(다른 DB로 검증하고 PostgreSQL 동작을 주장하면 거짓) ㉘**동시성 테스트** ← 정합성 주장의 유일한 실증

**E. API·운영** ㉙422와 400을 어떻게 나누나(같은 성격 오류가 같은 코드로 나가는가) ㉚v2는 언제 만드나 ㉛다른 사람이 30분 안에 띄울 수 있나 ㉜실수로 개인정보가 들어오면 어떻게 찾아 지우나(원문 미저장이 값을 한다. **그런데 로그는? 프롬프트는?**) ㉝배포 롤백 시 DB는

### 세부기술서 결함 10건 (`/design`에서 해소)

출처: `01-한화금융/Loan_포트폴리오_디벨롭_세부기술서.md` 전수 검토(2026-08-26).

| # | 결함 | 위치 |
|---|---|---|
| D1 | 판정 라벨 표기 분열 — `승인가능·상담필요·어려움`(§8.1) vs `"ELIGIBLE"`(§9.1) | §8.1·§9.1 |
| D2 | `RECEIVED`가 죽은 표면 — §10.1 단일 트랜잭션이라 커밋 시점에 이미 `SCREENED` | §7.1·§10.1 |
| D3 | "현재 유효한 설명"을 가리키는 것이 없음 — 재시도로 `explanation_run` N건이 되는데 유효본 표시 부재 | §8.1·§9.3 |
| D4 | Eval과 통합테스트의 축 오배치 — §13.2가 타임아웃·스키마 실패를 Eval에 넣었으나 §17.4는 녹화 픽스처. §13.4가 이미 모순을 인정 | §13.2·§13.4·§17.3 |
| D5 | 성능 실험 실행 불가 — §8.2가 인덱스를 미리 정의해 "전후 비교"가 성립 안 함 | §8.2·§18 |
| D6 | 조회 API 접근 통제 부재이며 비범위로도 미기재 → 면접 방어질문에 그대로 노출 | §9.1·§14.3·ADR-009 |
| D7 | UI 실행 경로 없음 — §5.1에 `ui` 모듈이 있으나 §16 Compose는 `app`·`postgres`뿐 | §5.1·§16·§19 |
| D8 | 중단 기준이 무력 — P0 12항목이 전부 축소 불가라 §11이 발동해도 줄일 것이 없다 | §11·§20 |
| D9 | 축 1 서사 부재 — §22 「강조할 설계 판단」 10개가 전부 기술 결정. "멀티에이전트를 무엇으로 이해했는가"가 없다 | §22 |
| D10 | 리스크 통제 대장 부재 — §14는 통제 목록이지 `리스크·성격·통제·기각 대안·검증 방법` 대장이 아니다 | §14·§21 |

### 신규 산출물 2건 (`/design`에서 명세에 포함)

- `docs/리스크_통제_대장.md` — `조건 · 적용 법령/필요 기술 · 데모의 범위 설계 · 전환 시 도입 검토 · 기각한 대안과 사유 · 검증 방법(테스트 ID)`. **검증 방법 칸이 핵심** — 통제를 적기만 하고 테스트가 없으면 문서일 뿐이다
- ADR 10건(세부기술서 §21)에 **「기각한 대안과 사유」 칸 필수화** — "합당한지"는 기각 사유에서만 나온다

## 선택 기법과 역할

### 왜 멀티에이전트인가 — D9 해소 (축 1 서사의 원본)

**흔한 오답:** "여러 에이전트가 협업하면 더 똑똑해진다." 이 서술은 도구 사용 보고이지 이해의 증거가 아니며, 포트폴리오가 실습 과제로 읽히는 지점이 정확히 여기다.

**이 프로젝트가 도달한 명제:**

> 에이전트 분리의 산물은 **지능이 아니라 검증 가능성**이다.
> 단일 프롬프트는 입력→출력 하나의 도약이라 **중간에 검사를 끼워 넣을 자리가 없다.**
> 경계를 만드는 목적은 그 경계마다 결정적 검증을 삽입할 수 있게 하는 것이다.

**증거 — 경계 3개마다 검증이 하나씩 들어가 있다.**

| 경계 | 삽입된 결정적 검증 | 실패 시 |
|---|---|---|
| 파싱 → 심사 | `rule_based_parse` 교차검증 + `missing_required_fields` 게이트 + 사용자 확정 폼 | 심사로 넘어가지 않는다(422) |
| 심사 → 안내 | `assess_loan_eligibility` 도구 강제 — CSV 밖 값 생성 경로 없음 | 판정이 바뀌지 않는다 |
| 안내 → 사용자 | Eval 6지표 + 디스클레이머 코드 강제 삽입 | 설명을 공개하지 않는다(`REVIEW_REQUIRED`) |

**그리고 여기서 인사이트가 나온다.**

> 멀티에이전트 설계를 끝까지 밀면, **가장 중요한 판정은 어느 에이전트에게도 주지 않게 된다.**
> 이 설계의 산출물은 「LLM이 하면 안 되는 일을 식별해 코드로 빼낸 것」이다.

3-Agent로 나눠본 결과가 `screen_loan`이라는 결정적 함수다. **"에이전트 3개를 썼다"와 "3개로 나눠 보니 판정은 에이전트에서 빼야 한다는 게 드러났다"는 층위가 다르다.**

**방어질문 ⑳(Agent를 2·4개로 바꾸면?)의 답이 여기서 나온다.** 경계 1개 = LLM 1턴 = 비용·지연이므로, 경계의 값어치는 **거기에 검증을 넣을 수 있는가**로만 정해진다. 검증을 넣을 수 없는 경계는 비용만 늘린다.

### 기법별 역할과 알려진 차이

| 기법 | 적용 위치 | 역할 | 정직하게 밝히는 차이 |
|---|---|---|---|
| 구조화 추출 | Agent 1 | 자연어 → 6필드 JSON | 권위값이 아니다. 규칙 파서 교차검증 + 사용자 확정을 거쳐야 심사에 들어간다 |
| CoT (4단계) | Agent 2 | DSR·신용등급·한도·상품선별 근거를 단계적으로 설명 | **판정을 만들지 않는다.** 도구가 낸 판정을 설명할 뿐이다 |
| Self-Consistency (응용) | Agent 2 | 보수·낙관·중립 3관점 교차검증 | **엄밀한 SC가 아니다.** 엄밀한 정의는 동일 질의를 N회 독립 추론 후 다수결이고, 이것은 한 응답 안에서 세 관점을 서술하는 응용이다. **최종 판정은 SC가 아니라 결정적 하드규칙이 내린다** |
| ReAct | Agent 3 | 금리·한도를 도구로 재조회해 확인 | 조회로 확인되지 않은 수치는 언급하지 않는다 |

**SC를 최종 판정에 쓰지 않은 이유(축 2의 핵심 인사이트).** 앙상블·다수결은 **오류가 서로 독립일 때만 안전하다.** 단일 모델의 오류는 상관관계를 가지므로 다수결이 틀린 답에 함께 수렴할 수 있다. 금융에서 이 실패 양상은 조용하고 일관되어 더 위험하다.

### 테스트 축 재배치 — D4 해소

**문제.** 세부기술서 §13.2가 「LLM 타임아웃·스키마 실패」를 Eval 케이스에 넣었는데, §17.4는 「API 키 없이 녹화 픽스처 평가」다. **녹화 픽스처로는 타임아웃을 만들 수 없다.** §13.4가 이미 "녹화 출력 평가는 실시간 제공자 장애를 검증하지 못한다"고 인정하고 있어 §13.2와 정면 충돌했다.

**해소 — 축을 나눈다.**

| 축 | 대상 | 방법 | 비용 |
|---|---|---|---|
| **Eval** (녹화 픽스처) | 출력 **내용**의 품질 — 파싱정확도·판정정합성·디스클레이머·추천정합성·수치근거·조건부표현 + 경계·적대적 케이스 | 저장된 출력을 규칙으로 채점 | API 0 |
| **API 통합 테스트** (주입된 실패) | 실패 **경로**의 처리 — 타임아웃·429·5xx·JSON 스키마 불일치 → 상태 전이·판정 보존 확인 | LLM 클라이언트를 대역으로 교체해 실패를 주입 | API 0 |

**판별 기준: 「출력이 있고 그 내용을 채점하는가」면 Eval, 「출력이 오지 않거나 형태가 깨졌을 때 시스템이 어떻게 되는가」면 통합 테스트.**

### 백엔드 채택 후보 판정 (`/design` §2 요구)

`reference/insights/backend-adoption-candidates.md` 4항목 전부 판정 완료. 사유는 [`docs/설계결정.md`](docs/설계결정.md) ADR-015~017.

| 항목 | 판정 | 요약 사유 |
|---|---|---|
| 목록 API 페이지네이션 | **채택**(커서 기반) | 기본은 보류이나 §18이 합성 이력 10,000건을 생성하므로 「쌓인다」 조건 충족. 명세에 아직 안 나가 계약 변경 비용 0 |
| 로그 로테이션 | **채택** | 비용 설정 몇 줄, 구조 영향 없음 |
| 서비스별 리소스 상한 | **조건부 채택** | `docker compose config`로 런타임 반영 확인 후 확정. 무시되면 다른 키로 교체하거나 보류하고 사유 기록 |
| 멱등 스키마 마이그레이션 | **보류** | Alembic 버전 테이블이 같은 보장을 제공. 같은 규칙이 두 자리에 생겨 갈라질 위험이 비용 |

### 하드닝 원칙 적용 (미적용은 결함)

`reference/insights/backend-hardening.md` 기준. 전 항목 적용 또는 사유 기록 완료 — 상세는 ADR-011·리스크 대장 B3·B4.

| 원칙 | 적용 |
|---|---|
| 계층별 네트워크 분리 · 데이터 계층 내부 전용 | ADR-011 — `postgres`를 `backend` 네트워크 전용 |
| 내부 서비스 호스트 포트 미게시 | ADR-011 — `postgres` 미노출. **`app` 노출은 타협이며 사유를 대장 B3에 기록** |
| CORS가 필요 없는 구조 | ADR-011 — Streamlit SSR이라 브라우저가 API를 직접 부르지 않는다 |
| 검증을 한 계층에만 두지 않음 | ADR-004 — 앱 선조회 + **DB UNIQUE 제약**. 우회 경로가 다르므로 중복이 아니다 |
| 파라미터 바인딩 | SQLAlchemy 사용. 문자열 포매팅 쿼리 금지 |
| 비밀값 상수 시간 비교 | 해당 표면 없음(토큰·관리자 키 비교 지점이 없다). 생기면 즉시 적용 |
| 배포 구성 파일도 코드 등급 | 인용부호 없는 목록형 환경변수 금지 — 게이트 `X1` |
| 관리 도구 기본 기동 제외 | DB 어드민 UI를 Compose 기본 서비스에 두지 않는다 |
| non-root + read-only 한 세트 | ADR-011 — 둘 다 적용. 쓰기 필요 경로만 개별 허용 |
| 헬스체크가 의존 자원 실제 호출 | ADR-012 방향 — `/health/ready`가 DB·마이그레이션 상태 확인. 현행 정적 `/health`(게이트 `I3`)를 대체 |

## 구현 상태

작업 브랜치: `feature/decision-support-rebuild` (저장소 관례가 `feature/*` → main 병합)

### 병렬 실행 계획 — Codex 협업 (2026-08-26 결정)

**속도 이득은 파일이 겹치지 않는 만큼만 나온다.** 이 프로젝트의 병목은 타이핑이 아니라 결정과 통합이므로, 같은 파일에 두 손이 들어가면 병합 충돌과 ADR로부터의 조용한 이탈이 생겨 오히려 느려진다. 그래서 **파일 소유권을 트랙별로 고정**한다.

| 트랙 | 담당 | 작업 | 소유 파일 |
|---|---|---|---|
| **A** | Claude(순차) | 영속화 체인 — 모델·Alembic·트랜잭션·Idempotency·상태 전이·LLM 실패 격리 | `loan_agent/api/**`, `loan_agent/db/**`, `alembic/**`, `tests/test_assessments*.py` |
| **B1** | Codex | Compose 3-tier + Dockerfile 하드닝 | `docker-compose.yml`, `Dockerfile`, `.dockerignore` |
| **B2** | Codex | CI `services: postgres` | `.github/workflows/ci.yml` |
| **B3** | Codex | Eval 케이스 확대 + 결함 주입 | `loan_agent/eval.py`, `tests/test_eval.py` |
| **C** | Codex(A 완료 후) | 부하 테스트 스크립트 · 문서 개편(`G3`·`D2` 해소) | `tests/load/**`, `README.md`, `docs/**` |

**A는 쪼개지 않는다.** 영속화 체인은 전부 같은 파일군을 건드려 병렬화하면 통합 비용이 이득을 넘는다.

**C를 A 뒤로 미루는 이유:** 문서가 코드보다 먼저 나오면 존재하지 않는 것을 설명하게 된다.

**심판은 게이트다.** 어느 트랙이 만들었든 `pytest`와 `python3.11 tools/gate.py .`로 같은 기준을 적용한다. 기존 BLOCK 4건(`G3`·`L1`·`S3`·`X1`)은 판정 완료분이며 **새로 생긴 BLOCK만 해당 트랙의 책임**이다.

넘길 프롬프트: `prompts/codex-track-b.md`. 하네스 원칙대로 **문서 내용을 프롬프트에 복사하지 않고 경로만 지정**했다 — 실행 주체가 `SPEC.yaml`·`docs/설계결정.md`·`PROJECT_LOG.md`를 직접 읽는다. 어느 쪽이 실행하든 같은 파일을 읽으면 같은 결론이 나온다.

**정직한 한계.** 통합 비용은 사라지지 않는다. 브랜치 병합과 게이트 재실행은 Track A가 한다. 15일이 7일로 줄지 않고, 대략 **12일 상당**으로 준다.

### CI 결정 — `services: postgres` 채택 (2026-08-26 승인)

| | Testcontainers | `services: postgres` |
|---|---|---|
| 컨테이너를 띄우는 주체 | 테스트 코드가 도커 API 호출 | CI 러너가 미리 기동 |
| 로컬 | 도커만 있으면 됨 ✅ | 별도 기동 필요 |
| CI | 도커 인 도커 — 느리고 설정 까다로움 | 3줄 ✅ |

**채택: CI는 `services`, 로컬은 compose.** 테스트 코드는 `DATABASE_URL`로 접속처만 받아 **양쪽에서 동일**하게 동작한다. ADR-008의 「실제 PostgreSQL」 조건은 어느 쪽이든 지켜지므로 ADR 개정이 아니라 그 안에서의 선택이다.

**띄우지 않았을 때의 손실이 결정 근거였다.** DB 없는 CI는 트랜잭션·Idempotency·UNIQUE 제약·상태 전이를 검증하지 못하는데 **그것들이 전부 P0-a**다. README 최상단에 CI 배지가 있으므로, 핵심 주장을 검증하지 않는 초록 배지는 증거가 아니라 장식이 된다. `skipif`로 건너뛰면 더 나쁘다 — **스킵은 초록으로 표시되어 아무도 눈치채지 못한다.**

healthcheck(`--health-cmd pg_isready`)를 필수로 둔다. 없으면 「컨테이너가 떴다」와 「연결을 받을 준비가 됐다」를 구분하지 못해 간헐적 실패가 난다.

### 로컬 sLLM — 전면 교체 기각, 비교 축으로 전환 (2026-08-26)

**제안:** 상용 LLM을 로컬 sLLM으로 교체하고 파인튜닝까지. 금융권이 데이터 외부 전송을 꺼린다는 점에서 방향은 정확하다.

**실측:** 기계는 M5·16GB로 7~8B 양자화 모델에 충분하다. 교체 지점도 `core.get_llm()` 한 곳이며 CrewAI가 LiteLLM을 쓰므로 `ollama/<model>`로 바꾸면 붙는다.

**기각 사유 — 도구 호출.** 이 아키텍처는 Agent 2가 `assess_loan_eligibility`를, Agent 3이 `lookup_loan_product`를 반드시 호출해야(ReAct) 성립한다. **7~8B 로컬 모델은 function calling이 불안정하다.** 3~5일을 쓰고도 아키텍처가 동작하지 않을 수 있다. 파인튜닝은 데이터셋·학습·평가·베이스라인 비교까지 최소 1~2주로 별개 프로젝트이며, 급조하면 「실습 과제」 인상을 오히려 강화한다.

**결정적으로 일정이 금지한다.** 잔여 15일에 전면 교체 4일 + 파인튜닝 1주를 넣으면 P0-a를 포기해야 하는데, §11이 **「P0-a가 위태로우면 기능이 아니라 제출 범위를 줄인다」**고 못박아 두었다.

**대안 — 선택 가능한 백엔드로 추가하고 Eval로 비교한다(2~3일, P0-b).**

「로컬로 바꿨다」보다 **「같은 Eval 지표로 상용과 로컬을 비교해 온프레미스 전환 시 품질 하락을 수치로 냈다」**가 높은 층위다.

1. Eval 하네스가 회귀 방지 도구에서 **의사결정 도구**로 올라간다(축 3 강화)
2. **관통 논리의 실증이 된다** — 설명 품질이 6/6 → 4/6으로 떨어져도 판정 정확도는 불변임을 표 하나로 보인다
3. 리스크 대장 A4의 ③칸(「온프레미스 LLM 검토」)이 문서에서 실측 근거로 바뀐다
4. **도구 호출이 실패해도 그것이 결과다** — 「8B 모델은 도구 호출 신뢰도가 낮아 현행 구조에 부적합했고, 그래서 판정을 코드가 갖는 설계가 온프레미스 전환에서 더 중요해진다」. 전면 교체였다면 같은 사실이 프로젝트 실패가 된다
5. 방어질문 ⑮의 답이 실측으로 채워진다. ADR-005가 `model_name`을 분리 저장한 이유가 여기서 회수된다

**착수 시점: P0-a가 궤도에 오른 뒤(9/3~5 감사·Eval 강화 구간).** Eval 케이스 확대와 겹쳐 비용이 준다.

**파인튜닝은 §7 10페이지 「검증된 범위·한계·입사 후 확장」으로.** 비교에서 관측한 품질 격차를 도메인 파인튜닝으로 좁히는 것이 다음 단계라고 쓰면, 근거 없는 희망이 아니라 **실측에 근거한 확장 계획**이 된다.

### 항목 1 — `GET /health/live` (완료, 2026-08-26)

**첫 항목이므로 관례를 확정했다.** 이후 항목이 이것을 복제한다.

| 확정한 관례 | 내용 |
|---|---|
| 파일 배치 | `loan_agent/api.py` 단일 모듈 → `loan_agent/api/` 패키지. **기능 단위 분리**(계층 아님). `uvicorn loan_agent.api:app` 임포트 경로는 유지 |
| 오류 응답 | `errors.py` — RFC 9457 `application/problem+json` 전 구간 통일. 상태코드별 제목을 한자리에 모아, 새 엔드포인트가 임의 코드를 쓰면 눈에 띄게 함 |
| 오류 본문 | 스택 트레이스·내부 경로 비노출. **Pydantic의 `input` 값을 버린다** — 그대로 되돌리면 요청에 담긴 소득·부채 금액이 응답과 로그에 되울린다 |
| 주석 | 태그·번호 없이 원인 → 과정 → 결론. 게이트 `L2` 0건 |
| 테스트 | 수용 기준을 명세에서 옮겨 적고 **실패를 먼저 확인**한 뒤 구현 |

**테스트 증거**

```
pytest tests/test_health.py   →  4 failed  (구현 전)
pytest tests/test_health.py   →  4 passed  (구현 후)
pytest                        →  47 passed
python3.11 tools/gate.py .    →  S4 0 · L2 0 · C3 0   (첫 항목에서 확인할 3축 전부 통과)
```

**함께 처리한 것**

- **구 API 표면 7개 전부 제거** → `S4` 7 → **0**. 명세에 없는 표면을 남기면 대조가 무의미해진다
- `test_api.py` 삭제, `test_demo.py`에서 API 테스트 3건 제거. **로직 커버리지 손실 없음** — `screen_loan`은 `test_screening.py`·`test_ranking.py`가 직접 덮는다
- **거짓 통과 1건 발견·제거:** `test_demo_case_out_of_range_404`가 라우트 제거 후에도 통과했다. `/demo/999`가 404인 이유가 「범위 밖」이 아니라 「경로 없음」으로 바뀌었는데 단언은 그대로였다. **검사하던 것을 더 이상 검사하지 않는 테스트**라 지웠다
- `test_command`를 `python3 -m pytest -q` → **`pytest`**로 교정. 이 환경의 `python3`는 3.14로 해석되어 프로젝트 의존성이 없다. CI가 쓰는 명령과 일치시켰고 옵션은 `pyproject.toml`에 있다

**게이트 `A5`(버전 접두사 불일치) — 의도된 비대칭, 사유 기록.** `/api/v1`은 **소비자 계약**이라 버저닝하고 `/health/*`는 오케스트레이터가 부르는 **운영 계약**이다. 프로브 URL이 API 버전을 따라 움직이면 버전을 올릴 때마다 배포 설정이 깨진다. 대상이 다르므로 축도 다르다. 사유는 `SPEC.yaml`에 남겼다.

**커버리지 부채(항목별로 상환 예정)**

| 잃은 검증 | 상환할 항목 |
|---|---|
| 자연어 파싱 HTTP 경로 | `POST /api/v1/parsing-preview` |
| 데모 케이스 목록·단건 조회 | `GET /api/v1/demo-cases` |
| 필수필드 누락 → 422, 키 부재 → 503 | `POST /api/v1/assessments` 및 설명 실행 항목 |

## 테스트 결과 · 증거 (출처 포함)
- (없음)

## 검증 결과 (`/verify`)
> 항목별 판정·근거 전문. `PROJECT_STATE.md`에는 판정값만 남긴다.

- (미실행)

## 뒤집힌 설계 결정
> `PROJECT_STATE.md`에서 내려온 결정. **무효화한 부속 규격과 그 처리 결과를 함께 적는다.**

| 결정 | 뒤집은 이유 | 무효화한 부속 규격 | 처리 결과 |
|---|---|---|---|
| - | - | - | - |

## 명세 개정 이력
> `/design` §5로 `SPEC.yaml`을 고칠 때마다 한 줄 추가한다.
> **이 표가 길어지면 설계 단계가 얕았다는 신호다.**

| 추가·삭제 항목 | 구분 | 사유(요구사항 지목) | 승인 | 시각 |
|---|---|---|---|---|
| - | 설계 누락 / 범위 확장 | - | - | - |

## 인사이트 수집 (`/insight add`)
> 이 프로젝트에서 나온 **재사용 가능한 판단**만 적는다. 사건 서술이 아니라 규칙 문장으로 쓴다.
> 하네스 자산으로 승격할지는 `/insight promote`가 판정한다. 승격 전까지는 이 프로젝트의 것이다.

- (없음)

## 폐기한 접근과 이유
- (없음)

## 포트폴리오 자동화 상태 (셀 상태 — `/compact`에서 보존)
- **대화 입력:** (없음 / 요약 파일 경로)
- **증거 추출:** 미실행
- **evidence.yaml:** 미생성
- **project.yaml:** 미생성 (status_source: )
- **PROJECTS.md:** 미생성
- **사용자 검토 승인:** 미승인 (승인 시각: )
- **발행 기준 검증(portfolio-criteria):** 미실행
- **보고서 프롬프트(/handoff report):** 미생성 (실행 주체: / prose-auditor 판정: )
- **공개 초안 프롬프트(/handoff publish):** 미생성 (실행 주체: / internal·public 구분: )
- **민감정보 스캔:** 미실행
- **외부 전송:** 미실행 (승인 시각: )
