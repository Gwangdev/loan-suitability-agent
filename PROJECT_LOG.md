# 프로젝트 기록 (누적 — 필요한 커맨드만 read)

> `PROJECT_STATE.md`의 참조용 짝. **매 단계 읽지 않는다.** 아래 「읽는 시점」에 해당할 때만 읽는다.
> 이 파일은 길어져도 된다. 대신 `PROJECT_STATE.md`를 짧게 유지한다.

## 읽는 시점

| 절 | 읽는 커맨드 |
|---|---|
| 피드백 대장 | `/verify`, `/handoff`, 피드백 번호를 부여·조회할 때 |
| 요구사항 변경 이력 | `/verify`, `/handoff` |
| 구현 상태 · 테스트 증거 | `/verify`, `/handoff` |
| 검증 결과 | `/verify`(write), `/handoff` |
| 명세 개정 이력 | `/design` §5(write), `/verify`, `/handoff` |
| 폐기한 접근 | 같은 논점이 다시 올라올 때 |
| 포트폴리오 자동화 상태 | `/handoff`(모든 유형) |
| 인사이트 수집 | `/insight`(read·add·promote) |

## 피드백 대장 (셀 상태 — 번호는 재사용·재정렬 금지)
> 출처와 무관하게 번호를 이어 붙인다. **코드에는 번호를 심지 않는다.**
> 지적과 코드의 대응은 이 표가 `커밋 해시`·`파일:라인`으로 갖는다(게이트 `L2`가 코드 내 태그를 BLOCK한다).
> **조치가 끝나도 행을 지우지 않는다.** 미조치 건만 `PROJECT_STATE.md`에 요약해 둔다.

| N | 출처 | 지적 | 조치 | 커밋 해시 | 파일:라인 | 회귀 테스트 |
|---|------|------|------|-----------|-----------|-------------|
| 1 | 본인 발견 | `[디벨롭: X]` 내부 태그가 공개 코드에 잔존. `d2f8a0d 공개 전 식별정보 제거`를 거쳤는데도 남았다 | **해소(2회에 걸침).** 1차로 `loan_agent/*.py` 29곳을 제거했으나, **검색 범위를 `loan_agent/`로만 잡아 「전부 제거」라고 보고한 것이 틀렸다.** `/build` 중 저장소 전체를 다시 훑어 `pyproject.toml`·`tests/test_products.py`·`.github/workflows/ci.yml`·`loan_agent_demo.ipynb` **4곳을 추가 제거**했다. 태그만 없애고 설명 문장은 유지 | 미커밋 | `loan_agent/*.py` · `pyproject.toml` · `tests/test_products.py` · `.github/workflows/ci.yml` · `loan_agent_demo.ipynb` | `grep -rn "\[디벨롭" .` → 0건 · `pytest` |
| 2 | 게이트 `X1` | `loan_agent/app.py:649` 하드코딩 자격증명 | **게이트 결함으로 확인·수정 완료.** 오탐이 아니라 게이트 버그였다 — v9.16이 추가한 따옴표 없는 대입 패턴(`ASSIGN_SECRET_BARE`)이 설정 파일용인데 소스 코드에까지 적용돼, 파이썬의 `api_key = _effective_api_key()` 같은 평범한 대입이 전부 걸렸다. **대상을 설정·환경 파일 확장자로 한정**했고 프로젝트 사본과 `~/.harness-src` 양쪽에 적용했다 | `aaacf11` | `tools/gate.py` | `test_gate.py` 「X1 반증: 소스의 함수 대입」 |
| 3 | 게이트 `G3` | `docs: 문서 문체를 학술체로 통일` 커밋이 저자 지문을 지웠다(213줄 삭제·232줄 추가, 정보량 동일) | **정정.** 앞서 「문서 개편에서 자연스럽게 해소, 개편 후 `G3` 소멸 확인」이라고 적었으나 **성립하지 않는다** — `G3`는 `git log`의 커밋 제목을 스캔하므로 문서를 다시 써도 과거 커밋(`e2455da`)은 이력에 영구히 남는다. 이력 재작성 없이는 사라지지 않으며, 그 재작성은 `P2`와 같은 이유로 기각했다. **조치: 게이트를 고쳤다(`aaacf11`)** — `--commit` 모드에서 정보로 강등해 커밋을 막지 않되, 일반 게이트에서는 BLOCK을 유지한다. 지적 자체는 유효하기 때문이다. 문서 개편은 별개로 진행하되 그것이 `G3`를 지우지는 않는다 | `aaacf11` | `tools/gate.py` | `test_gate.py` 「커밋: 과거 문체 커밋 비차단」 |
| 4 | 게이트 `I3` | `/health`가 정적 응답 — 의존 자원이 죽어도 healthy | **해소.** 항목 1에서 `/health/live`(정적, 의도적), 항목 2에서 `/health/ready`가 DB 연결 + Alembic head 대조를 실제로 수행. 준비 안 됨 → 503. 구 정적 `/health`는 항목 1에서 제거됨 | 미커밋 | `loan_agent/api/health.py` · `loan_agent/db/readiness.py` | `test_health.py` 「ready 503 when db unreachable / migration behind」 |
| 5 | 게이트 `D2` | `README.md`에 한계·미해결 절이 없다 | **진짜. 미조치 — `/design`에서 처리.** 전략기획서 §7 10페이지의 「검증된 범위·한계」·§12 금지 과장과 직결된다 | — | `README.md` | `gate.py` `D2` |
| 6 | 게이트 `P2` | 커밋 author 이메일 `rhkdzhf54@naver.com` 노출 | **범위 축소 확인 후 미조치 결정.** git 설정은 로컬·전역 모두 이미 `59742337+Gwangdev@users.noreply.github.com`이라 **앞으로의 커밋은 전부 noreply로 나간다.** 남은 것은 기존 28개 커밋의 author 값뿐이며, git은 author를 커밋 객체에 저장하므로 설정 변경이 소급되지 않는다. 이력 재작성(`filter-repo --mailmap`)은 해시 28개가 전부 바뀌고 force push가 필요해 되돌릴 수 없다. **실익(스팸 수집 방지)이 대가보다 작고, 「과거를 지우지 않고 앞으로를 바르게 한다」는 `G3` 처리 원칙과 일관되므로 그대로 둔다** | — | git 이력(28커밋, 2026-07-27) | — |
| 7 | 게이트 `S2` | 미호출 공개 함수 3건 | **전부 설명 가능. 미조치.** `run_logic_selftest()`=노트북·문서에서 호출하는 공개 API / `tool()`=crewai 부재 시 no-op 데코레이터 폴백(데코레이터로 실제 사용) / `worker()`=`threading.Thread(target=worker)`로 호출. 축 7은 정적 참조만 세므로 WARN | — | `loan_agent/core.py`·`app.py` | — |
| 8 | 게이트 `C1` | 인라인 주석 중 '왜'형 52/158 = 33% (목표 40%) | **미조치 — 관찰 중.** 하네스 README가 임계값을 잠정이라 명시(3~4회 사용 후 조정). 재작성 이후 재측정한다 | — | `loan_agent/` | — |
| 9 | 설계 검토 §6 | **T1 #6이 LLM 재시도를 직접 구현하겠다고 적혀 있었다.** 검토 기준(「재시도·서킷브레이커를 직접 구현하지 않았는가」)과 `CLAUDE.md`(「프레임워크 제공 기능 기본 채택」)를 동시에 위반 | **해소.** SDK 내장 재시도 채택 + 횟수·대기 상한 명시로 정정(ADR-018). **내가 만든 결함이며, 1차 검토(요청한 것이 동작하는가)로는 보이지 않고 2차 검토(관례 대비)에서만 나왔다** | — | `docs/설계결정.md` ADR-018 | 재시도 상한 통합 테스트 |
| 10 | 설계 검토 §4 | **ADR-013이 `current_explanation_run_id`를 도입하며 불변 심사 레코드에 유일한 가변 필드를 만들었는데, 갱신 경쟁을 다루지 않았다.** 재시도 두 건이 동시에 완료되면 lost update | **해소.** `PENDING`·`RUNNING` 실행이 있으면 409, 부분 UNIQUE 인덱스로 DB 강제, 갱신 시 심사 행 잠금(ADR-019) | — | `docs/설계결정.md` ADR-019 | 병렬 재시도 요청 → 1건만 수용 |
| 11 | 설계 검토 §3·§5 | 오류 정책이 불완전 — 빈 컬렉션 처리·오류 본문 형식·목록 limit 상한·필터 화이트리스트 미정 | **해소.** `SPEC.yaml`에 명시 — 빈 결과는 200+빈 배열(404·204 아님), RFC 9457 `problem+json` 전 구간 통일, limit 기본 20·상한 100을 **서버가 강제**, status·기간 필터를 허용목록 대조하고 컬럼명·ORDER BY로 흘리지 않음 | — | `SPEC.yaml` | 목록 API 통합 테스트 |
| 12 | 설계 검토 §1 | `GET /api/v1/products`의 요구사항(R8)이 **이번 설계에서 새로 만들어졌다.** 기존 `/products`를 살리려는 사후 정당화일 가능성 | **해소 — 제거(2026-08-26 사용자 승인).** R8은 사용자 시나리오가 아니라 **기존 엔드포인트에서 역산**됐고 그것이 사후 정당화의 표식이다. 감사는 결과에 저장된 `product_dataset_version`으로 충족되고, 상품 단건 조회는 LLM 도구 경로에만 필요하며 이는 HTTP 표면이 아니다. **R8 번호는 공백으로 남겨 제거 사실이 보이게 했다.** 필요해지면 §5 명세 개정으로 사유와 함께 재등재 | — | `SPEC.yaml` | — |
| 13 | independent-verifier | **README가 재구축 이전 표면을 문서화한다.** API 표와 curl 예시가 `GET /health`·`GET /products`·`GET /demo`·`POST /parse`·`POST /screen`·`POST /advise`를 제시하는데 코드에 그 6개는 없다(현 표면은 `SPEC.yaml`의 9개). PostgreSQL·Alembic·워커·compose 실행 절차가 없고 `DATABASE_URL`이 `.env.example`에도 없다. `README.md:236` 「Docker화 — 로컬 빌드·기동 검증 완료」는 C4 미실행과 정면 충돌 | **결함 확인. 미조치 — `/build`로 복귀해 재작성.** 직접 대조로 6개 엔드포인트 부재와 문구 충돌을 확인했다. P0-a #8과 완료 기준 「실행·재현·복구 절차」가 미충족이다 | — | `README.md:105-117` · `README.md:236` | README 표면 목록과 `SPEC.yaml` 대조 |
| 14 | independent-verifier | **CI는 재구축 이후 한 번도 돌지 않았고, 돌면 실패한다.** `ci.yml`의 설치 목록에 `streamlit`이 없는데 `tests/test_ui_api_client.py`가 `loan_agent.app`을 임포트하고 `app.py:37`이 `import streamlit`을 무조건 실행한다 → 수집 단계에서 ImportError. 최신 워크플로 실행은 2026-07-27(재구축 전) | **결함 확인. 미조치 — `/build`로 복귀.** `ci.yml:43` 설치 목록에 streamlit이 없음을 직접 확인했다. 「초록 배지가 장식이 되지 않게 한다」는 CI 결정의 논지가 현재 성립하지 않는다 | — | `.github/workflows/ci.yml:43` · `loan_agent/app.py:37` | CI에서 `pytest` 수집 성공 |
| 15 | independent-verifier | **컨테이너 이미지에 마이그레이션이 없고, readiness가 503 대신 500을 낸다.** `Dockerfile`의 COPY에 `alembic/`·`alembic.ini`가 없고 compose의 어느 서비스도 `alembic upgrade head`를 실행하지 않는다. 그 상태에서 `readiness.check()`의 `_expected_head()`가 try 블록 **밖**이라 `CommandError`가 그대로 올라가 마스킹 500이 된다. HEALTHCHECK는 Streamlit 8501을 찌르는데 app·worker가 같은 이미지라 worker는 영구 unhealthy | **결함 확인. 미조치 — `/build`로 복귀.** `Dockerfile:24-26` COPY 목록과 `readiness.py:29-41`의 try 범위를 직접 확인했다. 「준비 안 됨이면 503」 정책과 설계 검토 §3(같은 성격 실패는 같은 코드)이 깨진다. `tests/test_health.py`는 DB 불통·버전 뒤처짐만 보므로 **코드와 테스트가 같은 사각을 공유한다** | — | `Dockerfile:24-26,36-38` · `loan_agent/db/readiness.py:41` | alembic 부재 시 `/health/ready` → 503 |
| 16 | independent-verifier | **UI의 안내문 생성이 워커를 우회한다.** `app.py:795`가 `_run_pipeline()`으로 LLM을 직접 호출해 `explanation_run`·`eval_result`에 한 줄도 남기지 않는다. 같은 클릭이 만든 `PENDING` 행은 워커가 또 실행하므로 같은 심사에 설명 2회·토큰 2배다. 같은 자리에서 예외 원문을 `st.code(str(e))`로 화면에 노출한다 | **결함 확인. 미조치 — `/design`으로 복귀.** 실행 주체를 워커로 단일화할지, UI 직접 호출을 표면으로 인정할지가 설계 판단이다. R7(「모든 설명 시도가 조회 가능」)과 축 4(버전 감사) 주장이 실제 경로와 어긋난다. 예외 원문 노출은 `errors.py`·`worker.py`가 금지한 것과 같은 성격이다 | — | `loan_agent/app.py:795,800-802` | 화면 실행 1회 → `explanation_run` 1행 |
| 17 | independent-verifier | **UI가 API의 추천 결과를 버려 화면 추천 블록이 죽었다.** `_screen_from_assessment`가 `추천상품: None`·`추천후보: []`·`적격상품: []`·`부적격사유: {}`를 하드코딩해, 추천 렌더 분기가 사전 녹화 데모 경로 외에는 도달 불가다. API는 `recommendations`를 실어 보내는데 쓰지 않는다 | **결함 확인. 미조치 — `/design`으로 복귀(살릴지 지울지 판정 필요).** `app.py:632-635`의 하드코딩을 직접 확인했다. 도달 불가 상태는 기능 아이디어가 아니라 발견 사항이다 | — | `loan_agent/app.py:632-635` · `loan_agent/api/assessments.py:163-165` | 추천 있는 심사 → 화면 추천 블록 렌더 |
| 18 | independent-verifier | **워커가 Eval 지표 하나를 무조건 통과로 덮어쓴다.** 주석은 「해당 없음을 통과로 기록하지 않는다」인데 바로 다음 줄이 `checks["파싱정확도"] = True`다. 워커 경로의 파싱정확도는 영원히 False가 될 수 없다. `tests/test_worker.py`가 `generate_explanation`·`score_explanation`을 전부 대역으로 바꿔 두 함수에 테스트가 0건이다 | **결함 확인, 단 원인 서술을 정정한다. 미조치 — `/debug` 경유 후 수정.** 주석은 「대조할 파싱 출력이 없다」고 적었으나 **사실이 아니다.** `generate_explanation`이 `core.run_service()`를 부르고 `core._result_dict`(`core.py:656-661`)가 `파싱결과`를 반환하므로 **Agent 1은 워커 경로에서도 이미 돌았고 출력이 존재한다.** `Explanation` 데이터클래스(`worker.py:46-52`)에 담을 필드가 없어 버려질 뿐이다. **따라서 「측정 불가능한 지표를 어떻게 처리할까」가 아니라 「이미 있는 값을 배선하지 않고 상수로 덮었다」이다.** 조치는 파싱 출력을 채점기까지 넘기는 것 — LLM 호출·토큰 증가 0, 스키마 변경 0. 「해당 없음으로 기록」안은 기각했다: 전제가 거짓인 데다 `parse_accuracy`가 `nullable=False`라 어차피 불리언을 써야 하므로 더 간단하지도 않다 | — | `loan_agent/worker.py:150-152` · `tests/test_worker.py:99-105,147-155` | 파싱정확도 미달 입력 → `passed=False` |
| 19 | independent-verifier | **토큰 추출식에 도달 불가 분기가 있다.** `getattr(...) or usage.get(...) if isinstance(usage, dict) else None`은 `(getattr or get) if isinstance else None`으로 묶인다. dict일 때 `getattr`는 항상 None이라 앞 항이 죽은 코드이고, dict가 아니면 토큰이 조용히 None이 된다 | **결함 확인. 미조치 — `/debug` 경유 후 수정.** `ast`로 파싱해 `IfExp(test=isinstance, body=BoolOp(Or))` 구조임을 직접 확인했다. C3에서 관측된 「토큰이 기록된 행 0건」이 이 자리와 겹친다 | — | `loan_agent/worker.py:125-128` | 객체형·dict형 usage 양쪽에서 토큰 기록 |
| 20 | independent-verifier | **SPEC의 오류 코드 정책 중 413·415·429가 어떤 경로에도 없다.** 실측상 `text/plain`은 422, 2MB 본문은 상한 없이 수용된다. 429는 Streamlit 세션 카운터에만 있어 **UI가 API를 호출하는 현재 구조에서 API 표면에는 실행 상한이 없다** | **결함 확인. 미조치 — `/design`으로 복귀.** 소스 전역에서 413·415·429가 `errors.py`의 제목 사전 3줄에만 있음을 직접 확인했다. 구현할지 명세에서 내릴지는 설계 판단이며, 명세를 코드에 맞춰 조용히 줄이지 않고 §5 개정 이력에 사유와 함께 남긴다 | — | `SPEC.yaml:69-72` · `loan_agent/api/errors.py:29-32` | 미지원 Content-Type → 415 |
| 21 | independent-verifier | **내부 태그가 제출물에 남아 있다.** 검증자는 2건으로 보고했으나 **직접 세니 15건**이다 — `[포트폴리오 정리]` 2건, `[피드백①~④]` 13건(전부 `core.py`). `PROJECT_LOG.md`는 「미조치, `/verify`로 넘긴다」인데 `PROJECT_STATE.md`는 「Open Labels: (none)」이다 | **결함 확인(보고보다 심각). 미조치 — `/build`로 복귀.** `grep -rn` 결과 15건이며 `CLAUDE.md`의 「제출물에 내부 표기 금지」 위반이다. 대장 #1이 `[디벨롭:` 문자열만 지웠고 `[피드백` 형태를 놓쳤다. **게이트 `L2`가 이 형태를 잡지 못한다는 것도 함께 발견 사항이다** | — | `loan_agent/core.py`(13곳) · `loan_agent/app.py:9` · `loan_agent/core.py:22` | `grep -rn "\[피드백\|\[포트폴리오" .` → 0건 · `test_gate.py` L2 반증 |
| 22 | independent-verifier | **같은 자원의 형제 응답이 비대칭이고, 그 미결이 닫히지 않았다.** POST는 `verdict`·`dsr`을 평면으로 주고 진행 중 실행 1건만 `explanation_run`으로, GET은 같은 값을 `decision` 중첩으로 주고 `explanation_runs` 전체를 준다. 「미결 — 재시도 항목(R6)에서 확정한다」가 항목 6에 기록되지 않았다 | **결함 확인. 미조치 — `/design`으로 복귀해 응답 형상 확정.** 두 핸들러의 반환 dict를 직접 대조했다. 사유 없는 형제 비대칭은 설계 검토 §1 기준 결함이다 | — | `loan_agent/api/assessments.py:156-169` vs `194-211` | POST·GET 응답 스키마 대칭 검사 |
| 23 | independent-verifier | **멱등 키의 의미가 문서·주석·코드 간에 갈린다.** docstring은 「클릭마다 새 심사를 식별한다」인데 코드는 정규화 payload의 `uuid5`를 쓴다 — 내용이 같으면 시점과 무관하게 영원히 같은 키다. 수치가 같은 **다른 상담 건**은 새 심사가 생기지 않고 과거 심사가 200으로 재생된다 | **결함 확인. 미조치 — `/design`으로 복귀(어느 쪽이 의도인지 결정).** 문서 서술과 docstring이 정반대이므로 코드만 고치면 어느 한쪽을 임의로 채택하게 된다 | — | `loan_agent/app.py:602,616` | 같은 수치·다른 상담 → 새 심사 생성 여부 고정 |
| 24 | independent-verifier | **죽은 표면에 판정이 붙어 있지 않다.** 게이트 `S2`가 8건인데 대장 #7은 3건만 판정했다. 신규 5건 중 `core.py:630 has_api_key()`는 **프로덕션 호출처가 없고 테스트만 호출한다**. 예외 처리기 3건은 등록형 오탐, `db/engine.py reset()`은 conftest 전용. `G4`·`I4`도 판정 기록이 없다 | **결함 확인. 미조치 — `/design`으로 복귀해 판정.** `grep -rn has_api_key`로 호출처가 `tests/test_keys.py`뿐임을 직접 확인했다. **미사용 코드는 항상 발견 사항이며 「살릴지 지울지」 판정이 필요하다** | — | `loan_agent/core.py:630` · `loan_agent/api/errors.py` · `loan_agent/db/engine.py` | — |
| 25 | independent-verifier | **워커 재점유 경로에 펜싱이 없다.** 200초 초과 `RUNNING`을 `PENDING`으로 되돌리지만 `_finish()`는 현재 상태를 확인하지 않고 덮어쓴다. 지연된 워커와 재점유한 워커가 같은 run·같은 case를 이중 기록할 수 있다. `tests/test_worker.py`의 「한 작업은 한 번만 실행된다」는 claim 경로에만 성립한다 | **결함 확인. 미조치 — `/debug` 경유 후 수정.** `_finish()`가 `run.status` 검사 없이 필드를 덮어씀을 직접 확인했다. ADR-023의 「Stale RUNNING은 PENDING으로 되돌린다」와 짝이 되는 펜싱이 빠져 있다 | — | `loan_agent/worker.py:84-102,157-200` | 재점유 후 구 워커의 `_finish`가 무시됨 |
| 26 | 본인 발견(`/design` 2차 검토) | **테스트가 코드와 같은 오해를 담고 있다.** `tests/test_ui_api_client.py:30`의 `test_submit_assessment_reuses_the_key_for_the_same_confirmed_form`이 같은 폼으로 두 번 제출하면 멱등 키가 **같아야 한다**고 단언한다. 이는 #23이 결함으로 판정한 「내용 해시를 멱등 키로 쓴다」를 테스트가 정답으로 고정해 둔 것이다. ADR-027을 적용하면 이 테스트는 실패한다 | **결함 확인. 미조치 — `/build`에서 ADR-027과 함께 뒤집는다.** 테스트를 조용히 고치지 않고 지적으로 등록한다. **고칠 때 이름까지 바꾼다** — 「같은 폼이면 키를 재사용한다」가 아니라 「같은 제출의 재시도는 키를 재사용하고, 새 클릭은 새 키를 만든다」가 검증 대상이다. 이번 개발에서 결함이 반복해 나온 자리(코드와 테스트가 같은 오해를 공유)가 여기서 또 확인됐다 | — | `tests/test_ui_api_client.py:30-50` · `loan_agent/app.py:616` | 새 클릭 → 새 키 · 같은 제출 재시도 → 같은 키 |
| 27 | 본인 발견(`/design` 전제 재검증) | **재구축이 공개 라이브 데모를 이미 끊었고, 아무도 기록하지 않았다.** README 배지와 `README.md:237`이 https://loan-suitability-agent.streamlit.app/ 을 완료 항목으로 광고한다. Streamlit Community Cloud에는 **API도 DB도 워커도 없다** — `API_BASE_URL`은 `http://127.0.0.1:8000`으로 기본값을 잡으므로(`app.py:53`) 그곳에서는 `_submit_assessment`가 반드시 실패한다. `main`의 app.py에는 `API_BASE_URL`이 없어 **현재 배포본은 재구축 이전 코드이고 그래서 아직 동작한다.** 이 브랜치를 main에 합치는 순간 공개 데모가 깨진다. **게다가 ADR-024(워커 단일화)를 적용하면 그곳에서 유일하게 동작하던 `_run_pipeline` 직접 호출마저 사라져 데모가 완전히 죽는다** | **결함 확인. 미결 — 사용자 결정 필요.** **이 전제를 검토하지 않고 ADR-024를 쓴 것은 내 누락이다.** 배포 대상을 확인하지 않고 실행 주체만 보았다. 결함 자체는 ADR-024와 무관하게 이미 존재한다(재구축이 UI를 API 의존으로 바꾼 시점에 발생). 후보: (가) 공개 데모를 포기하고 README의 배지·완료 표기를 내린다 (나) API 불통 시 화면이 로컬 단독 모드로 물러나게 한다 — **단 그것은 ADR-024가 방금 없앤 이중 실행 경로를 되살린다** (다) 전체 스택을 배포할 곳을 따로 마련한다. **`/build` 착수 전에 정해야 한다 — ADR-024와 `#13` README 재작성이 둘 다 이 답에 걸려 있다** | — | `README.md:4,6,237` · `loan_agent/app.py:53` · `docker-compose.yml` | 공개 데모 경로의 동작 범위를 명시하는 문서 검증 |
| 28 | 본인 발견(전제 재검증) | **재구축이 「방문자 키 입력」 기능을 이미 죽였다.** 워커는 `core.run_service(sentence)`를 키 인자 없이 부른다(`worker.py:117`) — **항상 서버 환경변수 키를 쓴다.** 방문자 키는 `ui` 프로세스의 세션 메모리에만 있고 워커는 다른 프로세스라 볼 수 없으며, 키를 DB에 넣는 것은 금지다. 지금 방문자 키를 소비하는 곳은 `_run_pipeline(nl, api_key=api_key)`(`app.py:805`) 하나뿐이고, **ADR-024가 그것을 지우면 소비처가 0이 된다.** `README.md:235`의 `[x] 방문자 키 입력 + 비용 보호장치`는 재구축 아키텍처에서 이미 거짓이다 | **결함 확인. 미결 — 사용자 요구로 방향은 정해졌으나 구조 미확정.** 사용자가 「방문자가 자기 토큰으로 시현할 수 있어야 한다. 아니면 껍데기다」를 요구로 확정했으므로 **ADR-024(워커 단일화)는 이 요구와 양립할 수 없다.** 키를 가진 프로세스가 실행해야 하고, 키가 HTTP로 도달할 수 있는 가장 안쪽이 `app`이다. 구체 구조는 `docs/긴급/미결이슈.md` 참조 | — | `loan_agent/worker.py:117` · `loan_agent/app.py:805` · `README.md:235` | 방문자 키 실행이 `explanation_run`에 기록되는지 |
| 29 | 본인 발견(`/design` 표준 검토) | **`decision_result.dsr`의 왕복 타입이 어긋난다.** `loan_agent/db/models.py:107`이 `dsr: Mapped[float] = mapped_column(Numeric, nullable=False)`인데, SQLAlchemy `Numeric`은 **`asdecimal=True`가 기본값**이라(2.0.51에서 확인) **읽을 때 `Decimal`을 돌려준다.** 쓸 때는 `float`가 들어가므로 타입 힌트는 왕복의 절반만 참이고, 읽은 값을 `float`와 섞어 산술하면 `TypeError`가 난다. 게다가 `Numeric`에 precision·scale이 없어 **저장 정밀도가 명시되지 않았다.** 현재 `tests/test_assessments.py:56`은 `body["dsr"] >= 0`만 보아 타입을 검증하지 않는다 | **결함 확인. 미조치 — `/build`.** 타입 힌트를 실제 왕복 타입에 맞추고 precision·scale을 명시한다. **왕복 타입을 확인하는 DB 통합 테스트를 함께 추가한다** — 없으면 같은 오류가 다시 들어온다. 근거는 ADR-032 §32.1 | — | `loan_agent/db/models.py:107` · `tests/test_assessments.py:56` | DB 왕복 후 `dsr` 타입·정밀도 단언 |
| 30 | 사용자 지적(`/design` 인자) | **리스크 통제 대장에 전자금융거래법이 없었다.** A6(감사 대응)가 「전자금융업에 해당하면 감독규정상 기록·보존 의무」라고만 적어 ①**하위 고시(전자금융감독규정)의 강제력이 어디서 오는지**(전자금융거래법 제21조의 위임) ②**무엇이 적용 조건인지**(제2조 제1호의 정의) 둘 다 비어 있었다. 하위 규정만 인용하고 상위 법률을 빠뜨린 구조적 누락이다 | **해소(2026-08-29).** 대장에 **A7 신설** — 제2조 제1호 정의의 네 요소를 하나씩 대조해 데모가 조건 밖임을 서술하고, A6는 A7을 참조하도록 고쳤다. **부수 발견:** 「종사자와 의사소통 없이 자동화된 방식」 요소가 성립하지 않는 이유가 **A1에서 자동화된 결정을 피하려고 내린 설계 판단과 같은 것**이다 — 사람을 판정 경로에 남긴 하나의 선택이 두 법의 조건 밖에 동시에 놓인다 | — | `docs/리스크_통제_대장.md` A6·A7·D절 | 조문 번호 확정 근거 표기 확인 |
| 31 | 사용자 지적(`/design` 인자) | **준거 표준의 범위가 확정되지 않았다.** 「IEEE 표준은 강제력이 없으나 금융 실무에서 중요하다」는 통념의 사실 여부가 미검증 상태였다 | **해소(2026-08-29) — 통념은 일반론으로는 부정확하다.** 국내 금융권이 실제로 요구하는 체계는 **전자금융감독규정·ISMS-P·ISO/IEC 27001·금융보안원 가이드**이며 검색된 자료 어디에도 IEEE가 등장하지 않는다. IEEE 소프트웨어공학 표준은 대부분 대체·폐지됐다(830→`ISO/IEC/IEEE 29148:2011`, 1471→`42010`, 829→`29119-3:2013`) — **「IEEE 표준」이라는 호칭 자체가 낡았다.** 다만 **IEEE 754는 실질적으로 걸린다**(금액의 이진 부동소수점 표현 오차). 판정과 준거 범위는 **ADR-033**, IEEE 754 적용 검토는 **ADR-032**, 거기서 나온 실제 결함은 `#29` | — | `docs/설계결정.md` ADR-032·033 | — |
| 32 | 본인 발견(`/handoff` 착수 전 실측) | **`#21`의 범위가 두 번 축소 보고됐다. 실측치는 제출물 `loan_agent/`에 19건이다** — `core.py` 15건(`[피드백①~④]` 13 + `[포트폴리오 정리]` 1 + `[타팀 피드백-2]` 1), `app.py` 4건(`[타팀 피드백-1]` 3 + `[포트폴리오 정리]` 1). 검증자 보고는 2건, `PROJECT_STATE`는 15건이었다. **별개로 하네스 `tools/`에 12건이 더 있다**(`gate.py` 주석 7 · `test_gate.py` 주석 5). `tools/`는 제출물이 아니지만 저장소를 공개하면 함께 보인다 | **`#21`의 조치 범위를 19건으로 확정.** `tools/` 12건은 **별건으로 분리한다** — 하네스는 `~/.harness-src` 원본과 프로젝트 사본이 갈리면 안 되는 공유 자산이라 이 저장소에서 단독으로 고치면 사본이 어긋난다. **해소(2026-08-29) — 사용자가 「원본 먼저」를 선택.** `~/.harness-src`에서 12건을 제거하고 (`gate.py` 7 · `test_gate.py` 5) 자체 테스트 91/91 통과를 확인한 뒤 `tools/` 2개 파일을 프로젝트로 복사했다(sha256 동일). 하네스 **v9.17 → v9.19**(오탐 4건 v9.18 + 표기 제거 v9.19 2커밋), CHANGELOG 기록. **표기만 걷어내고 설명 문장은 남겼다** — 전부 「예전엔 …」 형태라 `C1`이 세는 '왜'형 자산이다. **작업 중 발견:** 게이트 `L2`가 이걸 못 잡은 것은 버그가 아니라 `is_deliverable()`이 하네스 자신의 파일을 제출물 후보에서 빼기 때문이고, 그 예외는 `b_leak_harness_ok`가 검증하고 있다. **예외 규칙은 옳으므로 그대로 뒀다** — 예외가 다루는 축(게이트가 BLOCK하지 않는다)과 이번 축(공개하면 보인다)이 다르다. `#21`의 19건은 여전히 별건이다. **`gate.py:1049`는 태그가 아니라 탐지 정규식이므로 절대 제거 금지** — 지우면 게이트가 이 결함군을 못 잡는다 | — | `loan_agent/core.py` · `loan_agent/app.py` · `tools/gate.py` · `tools/test_gate.py` | `grep -rn "\[피드백\|\[타팀 피드백\|\[포트폴리오 정리\]" loan_agent/` → 0건 |
| 33 | 본인 발견(Codex 결과 2차 검토) | **안 B 이행으로 죽은 코드가 생겼다.** `app.py:_run_pipeline`(58줄) 호출 0 — 화면이 API를 부르게 되면서 대체됐다. 그것만 부르던 `core.run_service_with_stages`도 함께 죽었고, `core.run_service`는 게이트 `S2`가 8→9로 잡았다. 3-Task 파이프라인(`parse_task`·`review_task`·`advise_task`)과 관련 Agent 정의도 서비스 경로에서 쓰이지 않는다 | **해소(2026-08-29) — 전부 삭제.** 사용자가 노트북 자체를 지우기로 정하면서 `#34`의 제약이 사라졌다. `core.py` **-255줄**(`lookup_product`·`screen_tool`·`build_reviewer_agent`·`build_advisor_agent`·`build_tasks`·`get_crew`·`_crew`/`_model_name`·`_result_dict`·`run_service`·`build_fresh_crew`·`run_service_with_stages` + 죽은 crewai `@tool` shim), `app.py` **-56줄**(`_run_pipeline`). **`build_parser_agent`·`get_llm`은 남긴다** — `parse_with_llm`(ADR-029)이 쓴다. **부수 발견:** 옛 파이프라인 호출을 감시하던 테스트 2건이 대상 부재로 깨졌다. **감시를 구조 단언으로 바꿨다**(`test_the_superseded_pipeline_is_gone_from_the_module`) — 호출 감시는 그 경로를 지날 때만 작동하지만 부재 단언은 어느 경로에서도 못 부른다는 뜻이라 보장이 더 강하다. **`#34`와 함께 결정해야 한다** — 노트북을 새 경로로 옮기면 전부 지울 수 있고, 노트북을 3-Agent 시연으로 남기면 그 이유를 문서에 적어야 한다 | — | `loan_agent/app.py:378` · `loan_agent/core.py` 3-Task 구간 | 게이트 `S2` 미호출 목록에서 제거 확인 |
| 34 | 본인 발견(Codex 결과 2차 검토) | **노트북이 폐기된 파이프라인을 시연한다.** `loan_agent_demo.ipynb`가 `run_service`(3-Agent·LLM 5회)를 그대로 돌린다. 서비스는 ADR-030으로 1회 호출이 됐으므로 **노트북과 서비스가 서로 다른 것을 보여준다.** 노트북은 포트폴리오 산출물이라 그대로 두면 「문서가 구현과 다르다」가 다시 생긴다 | **해소(2026-08-29) — 노트북 삭제.** 두 후보 모두 「노트북 유지」를 전제했는데 **그 전제를 먼저 검토했어야 했다.** markdown 23셀 6,006자를 전수 확인하니 **전부 목차·절 제목**이고 살릴 산문이 없었다. 뼈대가 `CrewAI 3-Agent`라 고쳐 쓸 대상이 아니라 사라진 문서였다. 노트북이 하던 검증(로직 자체테스트·엣지케이스·필수입력)은 `test_screening`·`test_parser`·`test_products`·`test_ranking` **32 passed**가 더 엄격하게 한다. **이전 설계는 git이 보존한다** — 56개 커밋에 남아 있고, 「`git log`에 있다」가 죽은 코드를 안고 있는 것보다 강한 답이다. 덤으로 `.ipynb`는 JSON 덩어리라 diff가 안 읽혀 히스토리를 리뷰 표면으로 쓰는 방침과 어긋났다. `requirements-dev.txt`의 `jupyter`도 제거 | — | `loan_agent_demo.ipynb` | 노트북 셀 실행 결과가 서비스 응답과 같은 형상인지 |
| 35 | 본인 발견(Codex 결과 2차 검토) | **`SPEC.yaml`에 없는 계약이 코드에 생겼다.** ①`X-OpenAI-API-Key` 헤더가 두 엔드포인트에 추가됨 ②같은 `POST .../explanation-runs`가 키 유무에 따라 **201 또는 200**을 낸다 ③`parsing-preview` 응답 형상 전면 변경(`candidate` → `rule_candidate`·`llm_candidate`·`mismatched_fields`·`parse_accuracy`·`degraded`) ④`run_payload`에 `explanation_text` 추가. **게이트 `S3`·`S4`는 경로만 대조하므로 원리상 못 잡는다** | **해소(2026-08-29).** `SPEC.yaml`에 네 계약을 모두 적었다 — ①키 헤더가 붙는 경로는 정확히 둘이고 어느 경로도 필수가 아니라는 것 ②201/200 분기와 그 근거(키 없으면 새 행 생성이라 201, 키 있으면 이미 있던 행을 집어 실행하고 그 상태를 돌려주므로 200. 클라이언트가 키 전송 여부를 알므로 예측 가능) ③`parsing-preview`의 6개 필드와 「불일치가 곧 검증」·「키 없으면 저하하되 실패하지 않음」 ④`explanation_text` 노출이 ADR-007과 충돌하지 않는 이유(미통과분은 애초에 기록되지 않아 NULL). **적은 내용을 코드와 대조 확인했다** — 응답 키 6개 일치, 201/200 분기 일치. 코드가 틀린 것이 아니라 **명세가 뒤처졌던 것**이다. 원인은 인계 프롬프트가 「`endpoints:` 수정 금지」라고만 써서 Codex가 문자 그대로 지킨 것이다 — **프롬프트의 결함이다.** `SPEC.yaml`은 헤더·응답 형상·오류 코드도 주석으로 관리하는 파일이므로 그 부분을 갱신해야 한다. ②의 201/200 분기는 클라이언트가 키 전송 여부를 알고 있어 예측 가능하므로 유지하되 **명시한다** | — | `SPEC.yaml` · `loan_agent/api/parsing.py` · `loan_agent/api/explanations.py` | 명세 주석과 실제 응답 형상 대조 |
| 36 | 본인 발견(Codex 결과 2차 검토) | **스키마를 바꿨는데 동결된 데이터모델 문서가 그대로다.** `decision_result.dsr`이 `Numeric` → `Numeric(18, 12)`로 바뀌고 마이그레이션 `0003_dsr_numeric_precision`이 신설됐는데 `docs/데이터모델.md`는 미변경이다. **인덱스 마이그레이션 2개를 병합하지 않은 것은 정상**이며 0003은 별개 파일이다 | **해소(2026-08-29).** ERD의 `dsr` 행에 `numeric(18,12)`와 출처 마이그레이션을 달고, §6에 **「마이그레이션 3 — `dsr` 저장 정밀도」**를 신설했다. **마이그레이션 2와 합치지 않는다** — 합치면 인덱스 전후 비교가 이 변경까지 끌고 들어간다. 정밀도를 명시하는 이유(임의 정밀도 `numeric`은 무엇이 저장되는지가 스키마에 없다)와 `#29`의 왕복 타입 수정, 그리고 **판정 계산은 여전히 `float`**라는 것을 함께 적었다. 이 스키마 변경 자체는 `#29` 조치에 필수적이므로 「동결 위반」이 아니라 **동결 문서의 갱신 누락**이다 | — | `docs/데이터모델.md` · `alembic/versions/0003_dsr_numeric_precision.py` | 문서의 ERD·마이그레이션 목록과 실제 파일 대조 |
| 37 | 본인 발견(`#33` 정리 중) | **코드 주석·docstring 11곳이 아직 「3-Agent」를 서술한다.** 성격이 둘로 갈린다 — ①파이프라인 자체를 서술한 것(`core.py` 모듈 docstring·`get_crew()/run_service()` 언급, `app.py` 모듈 docstring·「이번 3-Agent 실행 토큰」)은 **이미 거짓** ②녹화 픽스처를 서술한 것(`core.py:324`·`app.py:672,783`·`eval.py` 3곳·`api/demo.py:1`)은 **`demo_fixtures.json`이 실제로 3-Agent 출력이라 재생성 전까지 사실** | **부분 해소.** ①4곳은 정정했다. **해소(2026-08-31)** — 픽스처를 재생성했으므로 ②7곳도 함께 정정한다. 원문: **②7곳은 `demo_fixtures.json` 재생성과 함께 고쳐야 한다** — 지금 고치면 문서가 파일보다 앞서 나가 또 거짓이 된다. **픽스처 재생성은 오너 키가 필요해 사용자만 할 수 있다.** 재생성 시 이 7곳을 같은 커밋에서 바꾼다 | — | `loan_agent/core.py:324` · `loan_agent/app.py:672,783` · `loan_agent/eval.py:1,3,217` · `loan_agent/api/demo.py:1` | `grep -rn "3-Agent" loan_agent/` → 0건 |
| 38 | 사용자 지적 | **모듈 분리 없이 한 파일에 몰려 있다.** 측정하니 ①`app.py`의 `_inject_apple_css`가 **217줄, 파일의 27%** — 파이썬에 박힌 순수 표현 데이터이고 `CLAUDE.md:32`의 「Style is a file, not code」를 어긴다 ②`core.py`가 **변경 이유 6개**(심사 규칙·CSV 스키마·파싱 정규식·프롬프트·데모 자료)를 한 파일에 담아 SRP 위반 ③`X-OpenAI-API-Key`가 두 라우터에 문자열로 중복. **게이트 `C3`(템플릿 복제)는 무발생** — 문자 단위 중복이 아니라 책임 혼재라 기계가 못 잡는 종류다 | **부분 조치.** ①**해소** — `loan_agent/static/apple.css`(209줄)로 분리, `lru_cache` 로더가 1회만 읽고 파일 부재 시 무스타일로 저하한다. `app.py` 798→602 ②**부분 조치** — LLM 경로만 `loan_agent/llm.py`로 분리했다. **크기가 아니라 경계가 이유다**: 관통 논리가 「판정은 코드, 설명은 LLM」인데 그 경계가 주석이 아니라 **파일 경계**로 드러나야 한다. `core.py`에 LLM 참조 **0건**이 됐다(355줄). 나머지 분할(products·screening·parsing)은 **미결** — 15개 파일 90개 사이트를 건드려 「순수 이동」이 아니게 되고, 355줄은 읽을 만하다. 배포 직전 대규모 임포트 변경은 회귀가 나오는 자리다 ③**해소** — `api/contract.py`(임포트 없는 잎 모듈)로 상수화. `api/__init__.py`에 두려다 **순환 임포트**가 났다(그 파일이 라우터를 임포트하고 라우터가 되받는다) | — | `loan_agent/static/apple.css` · `loan_agent/llm.py` · `loan_agent/api/contract.py` | `grep -c "crewai\|Agent(\|Crew(" loan_agent/core.py` → 0 |
| 39 | 본인 발견(`#38` 작업 중) | **테스트 대역이 `raising=False`로 실패를 조용한 무동작으로 바꾸고 있었다.** `llm.py` 분리 후 `monkeypatch.setattr(core, "parse_with_llm", ..., raising=False)` 4곳이 없는 속성을 겨냥하게 됐는데, **오류 대신 대역이 안 붙고 실제 LLM이 호출됐다.** crewai 이벤트 로그로 발견했다 — 테스트는 「통과」로 보였을 수도 있는 상황이다 | **해소.** 대상을 `llm`으로 옮기고 **`raising=False`를 걷었다.** 그 플래그는 「속성이 없어도 넘어간다」는 뜻이라 **이름이 바뀌는 순간 보호가 조용히 사라진다.** 대역은 대상이 실재해야 의미가 있으므로 없으면 터지는 편이 맞다. 남은 `raising=False`는 전부 `delenv`(환경변수는 없을 수 있다)라 정당하다 | — | `tests/test_parser.py` · `tests/test_worker.py` · `tests/test_assessments.py` | LLM 미호출 경로에서 crewai 이벤트 0건 |
| 40 | `/code-review` | **워커가 제공자 타임아웃 한 번에 죽는다.** `execute_claimed_run`의 `except TimeoutError` 분기에 `return`이 없어, `raise_timeout=False`인 워커 경로에서 `_finish`로 흘러내려 할당된 적 없는 `explanation`·`score`를 읽는다. `UnboundLocalError`가 `run_once`→`main()`의 `while True`까지 올라가는데 거기 `try/except`가 없어 **프로세스가 종료된다.** 바로 아래 `except Exception` 분기에는 `return`이 있어 대비가 드러난다 | **고침.** `return`을 추가하고 왜 필요한지를 주석으로 남겼다. **제공자 타임아웃 테스트가 하나도 없어서** `89→103 passed` 내내 안 드러났다 — 워커 경로·동기 경로 두 방향 회귀 테스트를 추가했고, `return`을 다시 빼면 정확히 그 테스트가 깨지는 것을 확인했다 | 미커밋 | `loan_agent/worker.py` `execute_claimed_run` | `test_provider_timeout_on_the_worker_path_does_not_crash_the_loop` · `..._visitor_path_still_raises` |
| 41 | `/code-review` | **`#19`의 근본 원인이 특정됐다.** 토큰 추출식의 삼항 조건이 연산자 우선순위상 **전체 식을 `dict`인 경우로 묶어**, 객체를 처리하려고 넣은 `getattr` 분기가 **도달할 수 없었다.** `generate_guidance`는 `crew.usage_metrics`(crewai `UsageMetrics` 객체)를 넘기므로 `isinstance(usage, dict)`가 항상 거짓 → 두 값이 영구 `None` | **고침 — `#19` 해소.** `_usage_tokens()` 헬퍼로 분리해 객체·dict 양쪽을 처리한다. crewai 필드명(`prompt_tokens`·`completion_tokens`)은 `UsageMetrics.model_fields`로 확인했다 — 추측하면 같은 자리에서 또 `None`이 된다. **픽스처가 dict라 기존 테스트로는 드러나지 않던 자리**라 객체 케이스를 명시적으로 단언하는 테스트를 넣었다 | 미커밋 | `loan_agent/worker.py` `_usage_tokens`·`generate_explanation` | `test_usage_tokens_reads_objects_and_dicts` |
| 42 | `/code-review` | **같은 POST가 키 유무에 따라 본문 형상까지 달랐다.** 동기 경로는 `run_payload` 전체를, 비동기 경로는 `{id, status}`만 반환해 클라이언트가 **자기 요청에 키를 넣었는지로 파싱을 분기**해야 했다. ADR-026이 심사 응답에서 결함으로 판정한 형제 비대칭과 같은 형태다 | **고침.** 비동기 경로도 `run_payload`를 반환한다. 실행 전이라 대부분 필드가 비어 오는데 **그 비어 있음 자체가 상태를 말한다.** `SPEC.yaml`에 이번에 적은 계약 주석이 상태 코드 분기만 서술하고 본문 형상 차이는 빠뜨렸던 것도 함께 정정했다 — 명세가 실제보다 좁게 적혀 있었다 | 미커밋 | `loan_agent/api/explanations.py` · `SPEC.yaml` | 기존 `test_assessment_followups` |
| 43 | `/code-review` | **클라이언트 타임아웃이 서버 상한과 같은 200초라 503 경로가 도달 불가.** 서버가 상한을 넘겨 `_finish`를 기록하고 503을 직렬화하는 시간만큼 응답은 항상 200초를 넘으므로, 화면이 먼저 끊고 일반 오류 문구를 띄운다. ADR-024 §24-R-b가 정한 503 안내가 사용자에게 닿지 않는다 | **고침.** `CLIENT_TIMEOUT_MARGIN_SEC = 10`을 더해 바깥 계층이 길어지게 했다. **ADR-022의 원칙은 바깥이 안쪽보다 길어야 한다는 것이지 같아야 한다는 것이 아니다** | 미커밋 | `loan_agent/app.py` | — |
| 44 | `/code-review` | **Eval 미통과 시 사유 없이 빈 화면.** ADR-007에 따라 미통과분은 저장되지 않아 `explanation_text`가 NULL인데 HTTP는 200이라 `raise_for_status()`를 통과한다. 방문자는 토큰을 쓰고도 아무 문장 없는 화면을 보고, 검증에서 걸렸다는 사실도 재시도하면 된다는 안내도 받지 못한다 | **고침.** `_withheld_message()`가 `status`와 `eval_result`로 사유를 만들어 경고로 띄운다. **미달 지표명까지 보여준다** — 응답에 이미 들어 있는데 쓰이지 않던 값이다 | 미커밋 | `loan_agent/app.py` `_withheld_message` | — |
| 45 | `/code-review` | **화면 13곳이 삭제된 「Agent 1/2/3」 구조를 사용자에게 주장한다.** 특히 `st.subheader("Agent 2 — 심사 결과")`는 심사를 LLM 에이전트가 한다고 읽히는데, 실제로는 `screen_loan`이 결정적으로 계산한다 — 이 서비스가 내세우는 「판정은 코드」와 정면으로 어긋난다. `STAGE_LABEL`은 삭제된 `on_stage` 콜백 전용이라 **죽은 상수**이기도 했다 | **고침.** 라벨을 실제 구조로 바꿨다 — 「입력 해석 — 규칙 파서 · LLM 파서 대조」/「적합성 판정 — 결정적 규칙 (LLM 미개입)」/「고객 안내문 — LLM 생성 · 공개 전 검사 통과분」. **화면이 관통 논리를 스스로 설명하게 됐다.** 죽은 `STAGE_LABEL`·`STAGE_NEXT` 제거, `AGENT_TIPS`→`WAIT_TIPS` 개명, 중복 팁 정리 | 미커밋 | `loan_agent/app.py` | `grep -c "Agent [123]" loan_agent/app.py` → 0 |
| 46 | `/code-review` | **UUID `id.desc()` 정렬은 최신순이 아니다.** `claim_for_visitor`가 in-flight 행을 고를 때 uuid4를 정렬해 임의 선택이 된다. 현재는 in-flight 행이 최대 하나라 증상이 없지만 코드는 「최신 행을 고른다」로 읽힌다 | **고침.** `started_at.desc().nullslast()`로 바꿔 의도와 동작을 일치시켰다. **지금 버그가 아니라는 이유로 두면, 재시도 정책이 완화되는 순간 같은 심사에 토큰을 두 번 쓰는 경로가 조용히 생긴다** — ADR-024가 애초에 없애려던 실패 양상이다 | 미커밋 | `loan_agent/worker.py` `claim_for_visitor` | — |
| 47 | 본인 발견(compose 최초 실측) | **클린 체크아웃에서 스택이 기동조차 못 한다.** `postgres:16-alpine`의 postgres는 **uid/gid 70**인데 compose가 `user: "999:999"`(데비안 이미지 값)를 지정했고 `tmpfs`의 `uid=999`도 같은 착오다. 컨테이너가 볼륨을 소유하지 못해 `initdb: could not change permissions of directory ... Operation not permitted`로 죽는다 | **고침.** `user: "70:70"`과 tmpfs `uid=70`으로 정정하고 「이미지 변형과 uid는 함께 정해야 한다」를 주석에 남겼다. **Track C에서 「Docker 부재로 미실행」으로 남겨 둔 항목이라 지금까지 한 번도 뜬 적이 없었다** — 하드닝 설정을 넣고 검증하지 않으면 이렇게 된다 | 미커밋 | `docker-compose.yml` postgres | `docker compose up -d` → 4개 서비스 healthy |
| 48 | 본인 발견(compose 최초 실측) | **컨테이너가 준비 상태에 도달할 수단이 없다.** `alembic.ini`·`alembic/`이 이미지에 복사되지 않아 `alembic upgrade head`가 `No 'script_location' key found`로 실패하고, 스키마가 없으니 `/health/ready`가 영구 503이다. **`#15`에서 붙인 healthcheck는 그 사실을 정직하게 보고했을 뿐이고, 진짜 원인은 마이그레이션을 돌리는 주체가 어디에도 없다는 것이었다** | **고침.** Dockerfile에 `alembic.ini`·`alembic/`을 복사하고, **한 번 실행하고 끝나는 `migrate` 서비스**를 추가해 `app`·`worker`가 `service_completed_successfully`를 기다리게 했다. **앱 기동 명령에 섞지 않았다** — 인스턴스가 둘 이상이면 같은 마이그레이션을 동시에 돌려 경쟁하고, 「기동 시 자동 생성하지 않는다」는 규칙과 경계가 흐려진다 | 미커밋 | `Dockerfile` · `docker-compose.yml` migrate | `/health/ready` → `{"status":"ready","migration":"ok"}` |
| 49 | 본인 발견(`llm.py` 분리 후 첫 실행) | **리팩토링이 `parse_with_llm`을 깨뜨렸다.** `core.py`에서 떼어낼 때 `re`·`json` 임포트가 따라오지 않아 `NameError`로 죽었다. **테스트가 이 함수를 전부 대역으로 바꿔 두어 아무도 눈치채지 못했고**, 오너 키로 픽스처를 굽는 순간 드러났다. 「순수 이동이라 로직이 안 바뀐다」던 `#38`의 주장이 여기서 반증됐다 | **고침.** 임포트를 보강하고 **모듈이 쓰는 이름이 그 모듈에서 해결되는지 검사하는 테스트**를 넣었다(`test_every_module_resolves_its_own_names`). 호출을 감시하는 대역은 함수 안의 임포트 누락을 원리상 못 잡으므로 **호출이 아니라 이름 해석을 검사**한다. `re` 임포트를 다시 빼서 그 테스트가 정확히 그 이름을 짚어 실패하는 것을 확인했다 | 미커밋 | `loan_agent/llm.py` · `tests/test_keys.py` | `test_every_module_resolves_its_own_names` |
| 50 | 본인 발견(픽스처 재생성 중) | **안내문에 영문 enum이 직역돼 나갔다.** `_guidance_context`가 DB의 `ELIGIBLE`·`COMFORTABLE`을 그대로 프롬프트에 넣어, 모델이 「적격」·「편안한」으로 옮겼다. 도메인 어휘는 「승인가능」·「여유」다. Eval **판정정합성이 15건 중 3건 실패**했다 | **고침.** `decision.py`에 역방향 매핑(`VERDICT_LABEL`·`BAND_LABEL`)을 두고 사용자용 문장으로 나가는 경로에서 되돌린다. **ADR-012가 「API는 영문 enum, 화면은 한글」이라고 이미 정해 둔 것을, 안내문 경로가 화면 쪽이라는 사실을 놓쳐 어겼다.** 재생성 후 판정정합성 15/15 회복 | 미커밋 | `loan_agent/decision.py` · `loan_agent/worker.py` · `scripts/regenerate_demo_fixtures.py` | Eval 판정정합성 15/15 |
| 51 | 본인 발견(픽스처 재생성 중) | **Eval 수치근거의 근거 집합이 실제 주입분보다 좁았다.** 최상위 추천 하나(`추천상품`)만 근거로 삼는데, ADR-025·030 이후 안내문 프롬프트에는 **적격 상품 전체가 주입**된다. 모델이 실제로 받은 CSV 값(B-02의 3.5%~6.0%, E-01의 4.0%~7.0%)을 인용해도 환각으로 채점돼 15건 중 3건이 거짓 실패했다 | **고침.** 근거를 `추천후보`(금리·한도를 가진 실제 주입분)로 맞췄다. **넓힌 것이 아니라 「모델에게 실제로 준 것」에 맞춘 것이다** — `적격상품`은 코드·이름·은행만 담은 요약이라 근거가 될 수 없었다. **채점기가 약해지지 않았음을 결함 주입 테스트 9건 통과로 확인**했다. 수치근거 15/15 회복 | 미커밋 | `loan_agent/eval.py` | `tests/test_eval.py` 결함 주입 9건 |
| 52 | **경계 검증이 실제로 잡은 첫 사례** | **LLM 파서가 금액을 10배 틀렸다.** 「계약직 소액」 케이스에서 입력 "부채 200만원"을 `200000`으로, "500만원"을 `500000`으로 읽었다. 규칙 파서는 각각 `2000000`·`5000000`으로 옳게 읽었다 | **확정 절차로 처리.** 재생성 스크립트가 실제 흐름(두 후보 → 사람 확정 → 심사)을 재현하도록 고쳐, 불일치 필드는 규칙 파서 값으로 확정하고 그 사실을 픽스처의 `parse_check`에 남긴다. **이것이 ADR-029의 값어치를 말이 아니라 데이터로 보여주는 증거다** — 두 파서를 나란히 세우지 않았다면 10배 틀린 금액이 그대로 녹화돼 데모에 실렸다. 포트폴리오에서 인용할 것 | 미커밋 | `loan_agent/demo_fixtures.json` `parse_check` · `scripts/regenerate_demo_fixtures.py` | Eval 파싱정확도 15/15 |
| 53 | 본인 발견(게이트 `S2` 판정) | **미호출 공개 함수 6건에 판정을 부여한다**(「살릴지 지울지」는 결정 사항이지 관찰이 아니다). 5건은 게이트 오탐이다 — 예외 처리기 3개는 `install()`이 `add_exception_handler()`로 **등록**하고, `reset`·`has_api_key`는 테스트가 쓴다. 게이트는 직접 호출만 세어 콜백 등록과 테스트 전용 함수를 구분하지 못한다. **나머지 1건 `core.run_logic_selftest`는 진짜 미사용이다** — 유일한 참조가 `tests/test_screening.py`의 주석 「기존 인라인 self-test를 pytest로 이관하고」이고, 마지막 소비자였던 노트북은 `#34`에서 지웠다 | **`run_logic_selftest` 삭제. 나머지 5건 유지(사유 기록).** 이관이 끝났는데 원본이 남은 경우다. 같은 검증을 `test_screening`·`test_parser`·`test_products`·`test_ranking` 32건이 더 엄격하게 한다 — 사람이 눈으로 보는 self-test와 CI가 강제하는 테스트는 보장 층위가 다르다. **오탐 5건은 게이트를 고치지 않는다**: `tools/`는 `~/.harness-src`와 공유되는 자산이고(`#32`), 콜백 등록까지 추적하려면 정적 분석 범위가 크게 늘어난다. `S2`는 BLOCK이 아니라 WARN이므로 사유 기록으로 충분하다 | 미커밋 | `loan_agent/core.py` | `grep -c run_logic_selftest loan_agent/` → 0 |

출처 예: `사용자` · `본인 발견` · `independent-verifier` · `외부 도구 리뷰` · `타팀 리뷰`.
미채택 지적도 남기고 조치란에 사유를 적는다.

## 승인된 요구사항 · 수용 기준 · 변경 이력

### 관통 논리 (2026-08-26 사용자 확정 — 임의 변경 금지)

> 신뢰할 수 없는 기술을 개선하기보다는, 확실한 결과를 보장받을 수 있도록 **기술 적용 범위를 조절**하고 **신뢰 가능한 다른 기술을 함께 사용**했다.

뺄셈과 덧셈은 쌍이다. "LLM에서 무엇을 뺐다"를 쓸 때는 반드시 "무엇이 그 자리를 맡았다"를 같은 문장에 붙인다. 범위 축소만 말하면 AI 불신으로 읽히고, 채운 기술을 함께 말해야 보장 수준에 따른 기술 배치로 읽힌다.

| 대상 | 개선하지 않은 것 | 적용 범위 조절 | 함께 쓴 신뢰 가능한 기술 |
|---|---|---|---|
| 판정 | LLM 정확도 튜닝 | LLM은 설명만 | `screen_loan` 결정적 함수(CSV 하드규칙) |
| 파싱 | LLM 파싱 신뢰도 | LLM 파싱은 후보까지 | 규칙기반 파서 교차검증 + 필수필드 게이트 + 사용자 확정 폼 |
| 인젝션 | 입력 필터 강화 | 판정을 LLM 밖에 배치 | 도구 강제 — CSV 밖 값 생성 경로 없음 |
| 수치 | 환각 억제 프롬프트 | 수치는 생성 대상이 아님 | ReAct 도구 조회 + Eval 수치근거 지표 |
| 품질 | 프롬프트 튜닝 반복 | 감으로 판단하지 않음 | Eval 하네스 + 결함 주입 역검증 |

### 서술 3단 구조 (법적 공백·기술 공백 공통)

```
① 실무에서 이 조건이면 이것이 적용·필요하다     ← 지식
② 본 데모는 그 조건 밖으로 범위를 설계했다        ← 판단
③ 실서비스로 전환하면 무엇을 검토·도입해야 하는가  ← 확장 이해
```

③이 없으면 "안 걸리게 피했다"로 끝나 소극적으로 읽힌다. 공백을 서술할 때 3단을 모두 채운다.

### 포트폴리오가 반드시 전달해야 할 4개 축 (2026-08-26 사용자 요구)

단순 서브에이전트 과제로 인지되면 안 된다.

1. **멀티에이전트 기술의 본질 이해** — 경계를 만드는 목적은 경계마다 결정적 검증을 삽입할 수 있게 하는 것. 산물은 지능이 아니라 검증 가능성
2. **금융 도메인 × 기술 결합 인사이트** — ①앙상블은 오류가 독립일 때만 안전(단일 모델 오류는 상관관계를 가져 SC 다수결이 틀린 답에 수렴 가능) ②인젝션 방어를 추가하지 않고 공격 표면을 제거 ③"동작한다"와 "맞다"는 다르다(간이 DTI는 부채 0이면 항상 여유로 나왔다)
3. **기술적 확장 시도** — Eval의 자랑거리는 30/30 통과가 아니라 **결함 7종 주입으로 평가기 자체를 검증**한 것
4. **리스크 → 안전장치 → 근거의 합당성** — 대안 비교 없는 선택은 근거가 아니다. ADR·리스크 대장에 **기각한 대안과 사유**를 필수 칸으로 둔다

### 기술 공백 처리 등급 (T1/T2/T3)

전부 구현할 수 없으므로 위 3단 구조로 나눈다. **T2는 "안 했다"가 아니라 "이 조건이면 필요해지고 그때 이렇게 한다"로 답하면 T1과 같은 무게를 갖는다.**

**T1 — 구현해 증거로 남긴다 (11건, 2026-08-26 승인)**

| # | 항목 | 근거 |
|---|---|---|
| 1 | docker-compose 네트워크 격리 | 게이트 축 I(`I1`·`I2`·`I5`)가 기계 검증 → 3-tier 경계가 증거로 남는다 |
| 2 | DB 커넥션 풀 설정 + 산정 근거 | **ADR-022에서 확정.** 커넥션은 동시 사용자 수가 아니라 **점유 시간**으로 정해진다(Little's Law). LLM을 트랜잭션 밖에 두면 100 req/s에 필요 커넥션 **1개**, 안에 두면 **약 2,000개** — `max_connections` 기본값 100의 20배라 어떤 풀 크기로도 못 버틴다. **이 대비가 ADR-003의 값어치다** |
| 3 | 커서 기반 페이지네이션 | OFFSET은 깊이에 따라 열화 |
| 4 | 복합 인덱스 + `EXPLAIN ANALYZE` 전후 | 인덱스를 나중에 추가하는 마이그레이션으로 분리해야 전후 비교가 성립(D5) |
| 5 | 타임아웃 계층 | 클라이언트 > 게이트웨이 > 앱 > DB 순으로 짧아져야 좀비 요청이 안 쌓인다 |
| 6 | LLM 재시도 — **SDK 내장 채택 + 횟수·대기 상한 명시** | **설계 검토 §6에서 정정(ADR-018).** 처음엔 「백오프 직접 구현」으로 적었으나, 직접 구현은 호출 체인의 계층마다 재시도가 겹쳐 지연이 곱해진다. 프레임워크 제공 기능 기본 채택 규칙에도 어긋났다 |
| 7 | 아웃박스 패턴 명시 | `explanation_run(PENDING)`을 같은 트랜잭션에 넣는 것이 사실상 outbox — 명시하면 강해진다 |
| 8 | 부하 테스트 1회 | 안 하면 "충분한가"에 답할 실증 근거가 없다 |
| 9 | **동시성 테스트** | 같은 Idempotency Key로 병렬 요청 N개 → 심사 1건만 생성되는지. **없으면 P0 핵심 주장이 무증거로 남는다** |
| 10 | 토큰 비용 모델링 | `usage_metrics`를 이미 수집 중(A7 미노출). 화면 노출 + README 표 |
| 11 | UUID PK 선택 근거 문서화 | UUIDv4는 B-tree 삽입 시 페이지 분할이 잦아 인덱스 팽창. 알고도 데모 규모라 채택했다고 쓰면 그 자체가 증거 |

**T2 — 판단으로 방어** 큐·워커 분리 / Redis 공유 rate limit(현재 `session_state` 기반이라 다중 인스턴스에서 무력화) / 파티셔닝·아카이빙 / 무중단 마이그레이션(expand-contract) / JWT·세션·WAF / SLO·알림 / 감사 로그 무결성(append-only·해시체인)

**T3 — 명시적 비범위** k8s · 멀티리전 · 서비스메시 · Kafka (세부기술서 §16에 기재됨)

### 측정 계획 — 부하(T1 #8)·토큰 비용(T1 #10)

두 항목은 결정이 아니라 **측정**이므로 ADR이 아니라 수용 기준으로 둔다. 다만 **무엇을 어떤 기준으로 재는지**를 미리 정하지 않으면 사후에 유리한 숫자만 고르게 된다.

#### 부하 테스트

**대상은 결정적 경로(`POST /api/v1/assessments`)뿐이다.** 설명 생성 경로는 LLM 제공자 지연이 지배하므로 **우리 시스템 성능을 측정하지 못한다** — 재면 제공자를 재는 것이다. 이 구분 자체가 방어질문 ①의 답이다.

| 항목 | 내용 |
|---|---|
| 지표 | p50 · p95 · p99 지연, 처리량(req/s), 오류율 |
| 조건 | 동시 10 / 50 / 100, 각 60초 |
| 함께 기록 | 하드웨어, 데이터 건수, 커넥션 풀 설정(ADR-022) |

**합격·불합격 기준을 두지 않는다.** 포트폴리오 규모에서 SLO를 선언하면 근거가 없다. 목적은 **어디서 꺾이는지(포화점)를 찾는 것**이고, 「충분한가」라는 질문에는 **「이 조건에서 여기까지는 견디고 그 이상은 측정하지 않았다」**로 답한다.

ADR-022의 계산(필요 커넥션 ≈ 1개)이 맞는지 여기서 검증된다. **풀이 포화되기 전에 다른 곳이 먼저 꺾이면 계산이 옳았다는 증거다.**

**절대값을 일반화하지 않는다.** 실행 환경과 데이터 건수를 반드시 함께 적는다.

#### 토큰 비용

`usage_metrics`는 이미 수집 중이고 `explanation_run`에 `input_tokens`·`output_tokens` 컬럼이 있다(데이터모델 §2). 남은 것은 노출이다.

| 항목 | 내용 |
|---|---|
| 산출 | 요청당 평균 입력·출력 토큰 → 단가 적용 → 요청당 비용 → 월 N건 추정 |
| 표시 | 화면 + README 표 |
| 단가 취급 | **상수로 분리하고 출처·기준일을 함께 적는다.** 단가는 바뀌므로 본문에 숫자를 박으면 문서가 조용히 틀려진다 |

**3-Agent 구조의 비용을 정직하게 드러내는 것이 목적이다.** 경계 3개는 LLM 3턴이고 ReAct 재조회가 더해진다. 방어질문 ⑳(Agent를 2·4개로 바꾸면?)에 **「경계 1개당 비용이 얼마인지」** 수치로 답할 수 있게 된다.

### P0 내부 우선순위 — D8 해소 (`/design` 2026-08-26)

**문제:** 세부기술서 §20의 P0가 12항목 전부 「축소 불가」로 지정돼 있어, §11 중단·축소 기준이 발동해도 줄일 것이 없었다. 통제 장치가 이름만 있고 작동하지 않는 상태였다.

**해소:** P0를 둘로 나누고, 축소 가능한 쪽에는 **하한**을 함께 적는다. 하한이 없으면 축소가 삭제가 된다.

**P0-a — 양보 불가.** 이것이 빠지면 포트폴리오의 주장 자체가 무너진다.

| # | 항목 | 없으면 무너지는 주장 |
|---|---|---|
| 1 | 결정적 판정과 LLM 설명의 **코드 경계** 분리 | 관통 논리 전체 |
| 2 | PostgreSQL + Alembic 마이그레이션 | 「DB 설계를 할 줄 안다」 |
| 3 | 심사·판정·추천 **단일 트랜잭션** 저장 | 「정합성을 보장한다」 |
| 4 | Idempotency(키+요청해시+UNIQUE) **+ 동시성 테스트** | 「중복 요청을 막았다」 — 테스트가 없으면 무증거(T1 #9) |
| 5 | LLM 실패 격리 — 판정 보존 | 「부분 실패가 오염되지 않는다」 |
| 6 | 규칙·상품·모델·프롬프트 **4종 버전 저장** | 「재현·감사 가능하다」(축 4) |
| 7 | Docker Compose 3-tier + 게이트 축 I | 「3-tier 보안을 안다」(방어질문 ③) |
| 8 | README가 실제 구현과 일치 | 전략기획서 §13 체크리스트 |
| 9 | 60~90초 영상 데모 | §7 — 영상이 **기본 증거**, 라이브는 보조 |

**P0-b — 축소 가능. 하한까지만 줄이고 0으로 만들지 않는다.**

| # | 항목 | 하한(여기까지는 반드시) |
|---|---|---|
| 10 | 감사 이벤트 · Correlation ID | correlation_id 로그 전파 + `audit_event` 액션 3종 |
| 11 | Eval 케이스 확대(10~15건) | 경계 5건 + **결함 주입 7종은 유지**(축 3의 핵심) |
| 12 | API 통합 테스트 | 정상 / 422 / 409 / 조회 4건 |
| 13 | 부하 테스트 | 1회 실행 수치만 있으면 됨(T1 #8) |
| 14 | T1 잔여 8건 | 구현이 어려우면 **문서화만** — 판단으로 방어(T2 방식) |

**축소 발동 순서(§11 트리거 시):** 14 → 13 → 12 → 11 → 10. **P0-a는 어떤 경우에도 축소 대상이 아니며, P0-a가 위태로우면 기능을 줄이는 게 아니라 제출 범위를 줄인다.**

### 방어 질문 33건 (코드리뷰 시점 교차검증용)

> 2026-08-26 확정. **프로젝트 디벨롭 이후 코드리뷰에서 이 목록으로 누락을 교차검증한다**(사용자 판단 — 코드 없이 질문만 늘리면 수확체감).

**A. 동시성·부하** ①동시 트래픽 대비책, 현재로 충분한가(병목은 DB가 아니라 LLM이 요청 수명을 지배 — `/advise`가 3-Agent 전체를 `await`로 10~30초 점유) ②실제 DB 연결 시 난이도 급증(N+1·OFFSET 열화·커넥션 고갈·격리수준·무중단 마이그레이션·이력 무한증가) ③3-tier 보안 고도화(본질은 네트워크 경계로 DB를 외부에서 못 닿게 하는 것. **CORS는 방어가 아니다 — 브라우저만 지키는 규약이라 서버 간 요청엔 없다**)

**B. AI·LLM 운영** ④LLM 429·5xx 재시도 횟수·간격 ⑤타임아웃 계층 ⑥Idempotency Key TTL(없으면 정합성 장치가 용량 문제로 바뀐다) ⑦트랜잭션 커밋과 비동기 발행의 원자성 ⑧인스턴스 2대면 rate limit은 ⑨복합 인덱스 컬럼 순서 근거 ⑩부하 테스트 여부 ⑪장애 시 어디부터 보나(SLO·알림 기준 없음) ⑫감사 로그를 관리자가 지우면 ⑬JSONB vs 정규화 ⑭배포 중 마이그레이션 실패 ⑮모델 교체 시 무엇이 깨지나 ⑯프롬프트 수정 후 Eval 통과 = 배포 가능인가(5케이스 통과는 5케이스 통과일 뿐) ⑰LLM-as-judge를 왜 안 썼나(판정자가 비결정적이면 회귀 판정도 비결정적) ⑱요청당 토큰 비용·월 1만 건 ⑲프롬프트가 길어지면(비용·지연 + lost-in-the-middle) ⑳**Agent를 2·4개로 바꾸면** ← 본질 이해의 최종 판별. "3개가 적당해서"는 실습 과제, "경계마다 검증을 하나씩 넣을 수 있어서"는 설계자

**C. 데이터 모델(SQLD 연결)** ㉑`recommendation`을 왜 별도 테이블로(JSONB엔 UNIQUE 제약을 못 건다) ㉒`decision_result`가 1:1인데 왜 분리(생명주기가 다르다 — 심사는 상태가 변하고 판정은 불변) ㉓정규화를 어디서 멈췄나 ㉔상품 데이터를 왜 CSV에(버전 고정·결정성. 실서비스면 DB로 가야 하고 그때 `product_dataset_version`은) ㉕**UUID PK 성능** ← 강한 카드

**D. 테스트** ㉖테스트 54개가 무엇을 보장하나(개수는 지표가 아니다. 무엇을 안 덮는지 말할 수 있는가) ㉗Testcontainers를 왜(다른 DB로 검증하고 PostgreSQL 동작을 주장하면 거짓) ㉘**동시성 테스트** ← 정합성 주장의 유일한 실증

**E. API·운영** ㉙422와 400을 어떻게 나누나(같은 성격 오류가 같은 코드로 나가는가) ㉚v2는 언제 만드나 ㉛다른 사람이 30분 안에 띄울 수 있나 ㉜실수로 개인정보가 들어오면 어떻게 찾아 지우나(원문 미저장이 값을 한다. **그런데 로그는? 프롬프트는?**) ㉝배포 롤백 시 DB는

### 세부기술서 결함 10건 (`/design`에서 해소)

출처: `01-한화금융/Loan_포트폴리오_디벨롭_세부기술서.md` 전수 검토(2026-08-26).

| # | 결함 | 위치 |
|---|---|---|
| D1 | 판정 라벨 표기 분열 — `승인가능·상담필요·어려움`(§8.1) vs `"ELIGIBLE"`(§9.1) | §8.1·§9.1 |
| D2 | `RECEIVED`가 죽은 표면 — §10.1 단일 트랜잭션이라 커밋 시점에 이미 `SCREENED` | §7.1·§10.1 |
| D3 | "현재 유효한 설명"을 가리키는 것이 없음 — 재시도로 `explanation_run` N건이 되는데 유효본 표시 부재 | §8.1·§9.3 |
| D4 | Eval과 통합테스트의 축 오배치 — §13.2가 타임아웃·스키마 실패를 Eval에 넣었으나 §17.4는 녹화 픽스처. §13.4가 이미 모순을 인정 | §13.2·§13.4·§17.3 |
| D5 | 성능 실험 실행 불가 — §8.2가 인덱스를 미리 정의해 "전후 비교"가 성립 안 함 | §8.2·§18 |
| D6 | 조회 API 접근 통제 부재이며 비범위로도 미기재 → 면접 방어질문에 그대로 노출 | §9.1·§14.3·ADR-009 |
| D7 | UI 실행 경로 없음 — §5.1에 `ui` 모듈이 있으나 §16 Compose는 `app`·`postgres`뿐 | §5.1·§16·§19 |
| D8 | 중단 기준이 무력 — P0 12항목이 전부 축소 불가라 §11이 발동해도 줄일 것이 없다 | §11·§20 |
| D9 | 축 1 서사 부재 — §22 「강조할 설계 판단」 10개가 전부 기술 결정. "멀티에이전트를 무엇으로 이해했는가"가 없다 | §22 |
| D10 | 리스크 통제 대장 부재 — §14는 통제 목록이지 `리스크·성격·통제·기각 대안·검증 방법` 대장이 아니다 | §14·§21 |

### 신규 산출물 2건 (`/design`에서 명세에 포함)

- `docs/리스크_통제_대장.md` — `조건 · 적용 법령/필요 기술 · 데모의 범위 설계 · 전환 시 도입 검토 · 기각한 대안과 사유 · 검증 방법(테스트 ID)`. **검증 방법 칸이 핵심** — 통제를 적기만 하고 테스트가 없으면 문서일 뿐이다
- ADR 10건(세부기술서 §21)에 **「기각한 대안과 사유」 칸 필수화** — "합당한지"는 기각 사유에서만 나온다

## 선택 기법과 역할

### 왜 멀티에이전트인가 — D9 해소 (축 1 서사의 원본)

**흔한 오답:** "여러 에이전트가 협업하면 더 똑똑해진다." 이 서술은 도구 사용 보고이지 이해의 증거가 아니며, 포트폴리오가 실습 과제로 읽히는 지점이 정확히 여기다.

**이 프로젝트가 도달한 명제:**

> 에이전트 분리의 산물은 **지능이 아니라 검증 가능성**이다.
> 단일 프롬프트는 입력→출력 하나의 도약이라 **중간에 검사를 끼워 넣을 자리가 없다.**
> 경계를 만드는 목적은 그 경계마다 결정적 검증을 삽입할 수 있게 하는 것이다.

**증거 — 경계 3개마다 검증이 하나씩 들어가 있다.**

| 경계 | 삽입된 결정적 검증 | 실패 시 |
|---|---|---|
| 파싱 → 심사 | `rule_based_parse` 교차검증 + `missing_required_fields` 게이트 + 사용자 확정 폼 | 심사로 넘어가지 않는다(422) |
| 심사 → 안내 | `assess_loan_eligibility` 도구 강제 — CSV 밖 값 생성 경로 없음 | 판정이 바뀌지 않는다 |
| 안내 → 사용자 | Eval 6지표 + 디스클레이머 코드 강제 삽입 | 설명을 공개하지 않는다(`REVIEW_REQUIRED`) |

**그리고 여기서 인사이트가 나온다.**

> 멀티에이전트 설계를 끝까지 밀면, **가장 중요한 판정은 어느 에이전트에게도 주지 않게 된다.**
> 이 설계의 산출물은 「LLM이 하면 안 되는 일을 식별해 코드로 빼낸 것」이다.

3-Agent로 나눠본 결과가 `screen_loan`이라는 결정적 함수다. **"에이전트 3개를 썼다"와 "3개로 나눠 보니 판정은 에이전트에서 빼야 한다는 게 드러났다"는 층위가 다르다.**

**방어질문 ⑳(Agent를 2·4개로 바꾸면?)의 답이 여기서 나온다.** 경계 1개 = LLM 1턴 = 비용·지연이므로, 경계의 값어치는 **거기에 검증을 넣을 수 있는가**로만 정해진다. 검증을 넣을 수 없는 경계는 비용만 늘린다.

### 기법별 역할과 알려진 차이

| 기법 | 적용 위치 | 역할 | 정직하게 밝히는 차이 |
|---|---|---|---|
| 구조화 추출 | Agent 1 | 자연어 → 6필드 JSON | 권위값이 아니다. 규칙 파서 교차검증 + 사용자 확정을 거쳐야 심사에 들어간다 |
| CoT (4단계) | Agent 2 | DSR·신용등급·한도·상품선별 근거를 단계적으로 설명 | **판정을 만들지 않는다.** 도구가 낸 판정을 설명할 뿐이다 |
| Self-Consistency (응용) | Agent 2 | 보수·낙관·중립 3관점 교차검증 | **엄밀한 SC가 아니다.** 엄밀한 정의는 동일 질의를 N회 독립 추론 후 다수결이고, 이것은 한 응답 안에서 세 관점을 서술하는 응용이다. **최종 판정은 SC가 아니라 결정적 하드규칙이 내린다** |
| ReAct | Agent 3 | 금리·한도를 도구로 재조회해 확인 | 조회로 확인되지 않은 수치는 언급하지 않는다 |

**SC를 최종 판정에 쓰지 않은 이유(축 2의 핵심 인사이트).** 앙상블·다수결은 **오류가 서로 독립일 때만 안전하다.** 단일 모델의 오류는 상관관계를 가지므로 다수결이 틀린 답에 함께 수렴할 수 있다. 금융에서 이 실패 양상은 조용하고 일관되어 더 위험하다.

### 테스트 축 재배치 — D4 해소

**문제.** 세부기술서 §13.2가 「LLM 타임아웃·스키마 실패」를 Eval 케이스에 넣었는데, §17.4는 「API 키 없이 녹화 픽스처 평가」다. **녹화 픽스처로는 타임아웃을 만들 수 없다.** §13.4가 이미 "녹화 출력 평가는 실시간 제공자 장애를 검증하지 못한다"고 인정하고 있어 §13.2와 정면 충돌했다.

**해소 — 축을 나눈다.**

| 축 | 대상 | 방법 | 비용 |
|---|---|---|---|
| **Eval** (녹화 픽스처) | 출력 **내용**의 품질 — 파싱정확도·판정정합성·디스클레이머·추천정합성·수치근거·조건부표현 + 경계·적대적 케이스 | 저장된 출력을 규칙으로 채점 | API 0 |
| **API 통합 테스트** (주입된 실패) | 실패 **경로**의 처리 — 타임아웃·429·5xx·JSON 스키마 불일치 → 상태 전이·판정 보존 확인 | LLM 클라이언트를 대역으로 교체해 실패를 주입 | API 0 |

**판별 기준: 「출력이 있고 그 내용을 채점하는가」면 Eval, 「출력이 오지 않거나 형태가 깨졌을 때 시스템이 어떻게 되는가」면 통합 테스트.**

### 백엔드 채택 후보 판정 (`/design` §2 요구)

`reference/insights/backend-adoption-candidates.md` 4항목 전부 판정 완료. 사유는 [`docs/설계결정.md`](docs/설계결정.md) ADR-015~017.

| 항목 | 판정 | 요약 사유 |
|---|---|---|
| 목록 API 페이지네이션 | **채택**(커서 기반) | 기본은 보류이나 §18이 합성 이력 10,000건을 생성하므로 「쌓인다」 조건 충족. 명세에 아직 안 나가 계약 변경 비용 0 |
| 로그 로테이션 | **채택** | 비용 설정 몇 줄, 구조 영향 없음 |
| 서비스별 리소스 상한 | **조건부 채택** | `docker compose config`로 런타임 반영 확인 후 확정. 무시되면 다른 키로 교체하거나 보류하고 사유 기록 |
| 멱등 스키마 마이그레이션 | **보류** | Alembic 버전 테이블이 같은 보장을 제공. 같은 규칙이 두 자리에 생겨 갈라질 위험이 비용 |

### 기술 선택 — `/design` 2026-08-29 라운드

**새로 추가하는 의존성이 없다.** 안 B(ADR-029·030)는 이미 있는 `crewai`/`openai` 경로를 재배치하는 것이고, 배포(ADR-031)는 이미 있는 `docker-compose.yml`을 옮기는 것이다. **직접 구현으로 전환한 항목도 없다.**

| 항목 | 판정 | 사유 |
|---|---|---|
| 수치 표현 — `Decimal` 전환 | **기각, `float` 유지** | 측정 결과 밴드 경계에서 판정이 동일하다. 1원 입력 격자(DSR 해상도 `1e-9`)가 binary64 오차(`1e-16`)보다 7자리 크다. **바꿔서 얻는 것이 없다.** 성립 조건을 ADR-032에 명시 |
| TLS 종료 — 리버스 프록시 | **채택(Caddy 등 기성품)** | 방문자 키가 평문으로 지나면 안 된다(ADR-031 §31.5). **자체 구현 대상이 아니다** — 인증서 발급·갱신은 기성품이 제공한다 |
| 오케스트레이션 — EKS/k8s | **기각** | ADR-001 전환 조건 4개가 하나도 성립하지 않는다(ADR-031 §31.2) |
| 스테일 `RUNNING` 회수 — 스케줄러 | **기각** | 동기 경로가 집을 때 함께 회수한다(ADR-024 §24-R-a). **새 프로세스도 새 숫자도 만들지 않는다** |

### 백엔드 채택 후보 재판정 — 배포 형상 변경분 (2026-08-29)

ADR-031로 배포 형상이 바뀌었으므로 영향받는 항목만 다시 본다.

| 항목 | 판정 | 사유 |
|---|---|---|
| Redis 공유 rate limit | **한화 제출본 보류 · 2차 디벨롭 채택** | 조건은 「인스턴스가 2개 이상」이고 ADR-031은 **단일 인스턴스**다. 조건이 아직 성립하지 않는다. **알려진 결함으로 대장 B5에 이미 기록돼 있으므로 은폐가 아니다.** 2차 때는 **인스턴스를 늘려 깨지는 것을 먼저 재현한 뒤** 도입한다 — 깨진 것을 보여야 도입이 정당해진다 |
| 로그 로테이션 | **채택 유지** | 변동 없음 |
| 서비스별 리소스 상한 | **채택 유지** | EC2 단일 인스턴스에서 오히려 중요해졌다. 한 컨테이너가 인스턴스를 잠식하면 전체가 죽는다 |
| 멱등 스키마 마이그레이션 | **보류 유지** | Alembic 버전 테이블이 같은 보장을 준다 |

### 하드닝 원칙 적용 (미적용은 결함)

`reference/insights/backend-hardening.md` 기준. 전 항목 적용 또는 사유 기록 완료 — 상세는 ADR-011·리스크 대장 B3·B4.

| 원칙 | 적용 |
|---|---|
| 계층별 네트워크 분리 · 데이터 계층 내부 전용 | ADR-011 — `postgres`를 `backend` 네트워크 전용 |
| 내부 서비스 호스트 포트 미게시 | ADR-011 — `postgres` 미노출. **`app` 노출은 타협이며 사유를 대장 B3에 기록** |
| CORS가 필요 없는 구조 | ADR-011 — Streamlit SSR이라 브라우저가 API를 직접 부르지 않는다 |
| 검증을 한 계층에만 두지 않음 | ADR-004 — 앱 선조회 + **DB UNIQUE 제약**. 우회 경로가 다르므로 중복이 아니다 |
| 파라미터 바인딩 | SQLAlchemy 사용. 문자열 포매팅 쿼리 금지 |
| 비밀값 상수 시간 비교 | 해당 표면 없음(토큰·관리자 키 비교 지점이 없다). 생기면 즉시 적용 |
| 배포 구성 파일도 코드 등급 | 인용부호 없는 목록형 환경변수 금지 — 게이트 `X1` |
| 관리 도구 기본 기동 제외 | DB 어드민 UI를 Compose 기본 서비스에 두지 않는다 |
| non-root + read-only 한 세트 | ADR-011 — 둘 다 적용. 쓰기 필요 경로만 개별 허용 |
| 헬스체크가 의존 자원 실제 호출 | ADR-012 방향 — `/health/ready`가 DB·마이그레이션 상태 확인. 현행 정적 `/health`(게이트 `I3`)를 대체 |

## 구현 상태

작업 브랜치: `feature/decision-support-rebuild` (저장소 관례가 `feature/*` → main 병합)

### 병렬 실행 계획 — Codex 협업 (2026-08-26 결정)

**속도 이득은 파일이 겹치지 않는 만큼만 나온다.** 이 프로젝트의 병목은 타이핑이 아니라 결정과 통합이므로, 같은 파일에 두 손이 들어가면 병합 충돌과 ADR로부터의 조용한 이탈이 생겨 오히려 느려진다. 그래서 **파일 소유권을 트랙별로 고정**한다.

| 트랙 | 담당 | 작업 | 소유 파일 |
|---|---|---|---|
| **A** | Claude(순차) | 영속화 체인 — 모델·Alembic·트랜잭션·Idempotency·상태 전이·LLM 실패 격리 | `loan_agent/api/**`, `loan_agent/db/**`, `alembic/**`, `tests/test_assessments*.py` |
| **B1** | Codex | Compose 3-tier + Dockerfile 하드닝 | `docker-compose.yml`, `Dockerfile`, `.dockerignore` |
| **B2** | Codex | CI `services: postgres` | `.github/workflows/ci.yml` |
| **B3** | Codex | Eval 케이스 확대 + 결함 주입 | `loan_agent/eval.py`, `tests/test_eval.py` |
| **C** | Codex(A 완료 후) | 부하 테스트 스크립트 · 문서 개편(`G3`·`D2` 해소) | `tests/load/**`, `README.md`, `docs/**` |

**A는 쪼개지 않는다.** 영속화 체인은 전부 같은 파일군을 건드려 병렬화하면 통합 비용이 이득을 넘는다.

**C를 A 뒤로 미루는 이유:** 문서가 코드보다 먼저 나오면 존재하지 않는 것을 설명하게 된다.

**심판은 게이트다.** 어느 트랙이 만들었든 `pytest`와 `python3.11 tools/gate.py .`로 같은 기준을 적용한다. 기존 BLOCK 4건(`G3`·`L1`·`S3`·`X1`)은 판정 완료분이며 **새로 생긴 BLOCK만 해당 트랙의 책임**이다.

넘길 프롬프트: `prompts/codex-track-b.md`. 하네스 원칙대로 **문서 내용을 프롬프트에 복사하지 않고 경로만 지정**했다 — 실행 주체가 `SPEC.yaml`·`docs/설계결정.md`·`PROJECT_LOG.md`를 직접 읽는다. 어느 쪽이 실행하든 같은 파일을 읽으면 같은 결론이 나온다.

**정직한 한계.** 통합 비용은 사라지지 않는다. 브랜치 병합과 게이트 재실행은 Track A가 한다. 15일이 7일로 줄지 않고, 대략 **12일 상당**으로 준다.

### CI 결정 — `services: postgres` 채택 (2026-08-26 승인)

| | Testcontainers | `services: postgres` |
|---|---|---|
| 컨테이너를 띄우는 주체 | 테스트 코드가 도커 API 호출 | CI 러너가 미리 기동 |
| 로컬 | 도커만 있으면 됨 ✅ | 별도 기동 필요 |
| CI | 도커 인 도커 — 느리고 설정 까다로움 | 3줄 ✅ |

**채택: CI는 `services`, 로컬은 compose.** 테스트 코드는 `DATABASE_URL`로 접속처만 받아 **양쪽에서 동일**하게 동작한다. ADR-008의 「실제 PostgreSQL」 조건은 어느 쪽이든 지켜지므로 ADR 개정이 아니라 그 안에서의 선택이다.

**띄우지 않았을 때의 손실이 결정 근거였다.** DB 없는 CI는 트랜잭션·Idempotency·UNIQUE 제약·상태 전이를 검증하지 못하는데 **그것들이 전부 P0-a**다. README 최상단에 CI 배지가 있으므로, 핵심 주장을 검증하지 않는 초록 배지는 증거가 아니라 장식이 된다. `skipif`로 건너뛰면 더 나쁘다 — **스킵은 초록으로 표시되어 아무도 눈치채지 못한다.**

healthcheck(`--health-cmd pg_isready`)를 필수로 둔다. 없으면 「컨테이너가 떴다」와 「연결을 받을 준비가 됐다」를 구분하지 못해 간헐적 실패가 난다.

### 로컬 sLLM — 전면 교체 기각, 비교 축으로 전환 (2026-08-26)

**제안:** 상용 LLM을 로컬 sLLM으로 교체하고 파인튜닝까지. 금융권이 데이터 외부 전송을 꺼린다는 점에서 방향은 정확하다.

**실측:** 기계는 M5·16GB로 7~8B 양자화 모델에 충분하다. 교체 지점도 `core.get_llm()` 한 곳이며 CrewAI가 LiteLLM을 쓰므로 `ollama/<model>`로 바꾸면 붙는다.

**기각 사유 — 도구 호출.** 이 아키텍처는 Agent 2가 `assess_loan_eligibility`를, Agent 3이 `lookup_loan_product`를 반드시 호출해야(ReAct) 성립한다. **7~8B 로컬 모델은 function calling이 불안정하다.** 3~5일을 쓰고도 아키텍처가 동작하지 않을 수 있다. 파인튜닝은 데이터셋·학습·평가·베이스라인 비교까지 최소 1~2주로 별개 프로젝트이며, 급조하면 「실습 과제」 인상을 오히려 강화한다.

**결정적으로 일정이 금지한다.** 잔여 15일에 전면 교체 4일 + 파인튜닝 1주를 넣으면 P0-a를 포기해야 하는데, §11이 **「P0-a가 위태로우면 기능이 아니라 제출 범위를 줄인다」**고 못박아 두었다.

**대안 — 선택 가능한 백엔드로 추가하고 Eval로 비교한다(2~3일, P0-b).**

「로컬로 바꿨다」보다 **「같은 Eval 지표로 상용과 로컬을 비교해 온프레미스 전환 시 품질 하락을 수치로 냈다」**가 높은 층위다.

1. Eval 하네스가 회귀 방지 도구에서 **의사결정 도구**로 올라간다(축 3 강화)
2. **관통 논리의 실증이 된다** — 설명 품질이 6/6 → 4/6으로 떨어져도 판정 정확도는 불변임을 표 하나로 보인다
3. 리스크 대장 A4의 ③칸(「온프레미스 LLM 검토」)이 문서에서 실측 근거로 바뀐다
4. **도구 호출이 실패해도 그것이 결과다** — 「8B 모델은 도구 호출 신뢰도가 낮아 현행 구조에 부적합했고, 그래서 판정을 코드가 갖는 설계가 온프레미스 전환에서 더 중요해진다」. 전면 교체였다면 같은 사실이 프로젝트 실패가 된다
5. 방어질문 ⑮의 답이 실측으로 채워진다. ADR-005가 `model_name`을 분리 저장한 이유가 여기서 회수된다

**착수 시점: P0-a가 궤도에 오른 뒤(9/3~5 감사·Eval 강화 구간).** Eval 케이스 확대와 겹쳐 비용이 준다.

**파인튜닝은 §7 10페이지 「검증된 범위·한계·입사 후 확장」으로.** 비교에서 관측한 품질 격차를 도메인 파인튜닝으로 좁히는 것이 다음 단계라고 쓰면, 근거 없는 희망이 아니라 **실측에 근거한 확장 계획**이 된다.

### 항목 1 — `GET /health/live` (완료, 2026-08-26)

**첫 항목이므로 관례를 확정했다.** 이후 항목이 이것을 복제한다.

| 확정한 관례 | 내용 |
|---|---|
| 파일 배치 | `loan_agent/api.py` 단일 모듈 → `loan_agent/api/` 패키지. **기능 단위 분리**(계층 아님). `uvicorn loan_agent.api:app` 임포트 경로는 유지 |
| 오류 응답 | `errors.py` — RFC 9457 `application/problem+json` 전 구간 통일. 상태코드별 제목을 한자리에 모아, 새 엔드포인트가 임의 코드를 쓰면 눈에 띄게 함 |
| 오류 본문 | 스택 트레이스·내부 경로 비노출. **Pydantic의 `input` 값을 버린다** — 그대로 되돌리면 요청에 담긴 소득·부채 금액이 응답과 로그에 되울린다 |
| 주석 | 태그·번호 없이 원인 → 과정 → 결론. 게이트 `L2` 0건 |
| 테스트 | 수용 기준을 명세에서 옮겨 적고 **실패를 먼저 확인**한 뒤 구현 |

**테스트 증거**

```
pytest tests/test_health.py   →  4 failed  (구현 전)
pytest tests/test_health.py   →  4 passed  (구현 후)
pytest                        →  47 passed
python3.11 tools/gate.py .    →  S4 0 · L2 0 · C3 0   (첫 항목에서 확인할 3축 전부 통과)
```

**함께 처리한 것**

- **구 API 표면 7개 전부 제거** → `S4` 7 → **0**. 명세에 없는 표면을 남기면 대조가 무의미해진다
- `test_api.py` 삭제, `test_demo.py`에서 API 테스트 3건 제거. **로직 커버리지 손실 없음** — `screen_loan`은 `test_screening.py`·`test_ranking.py`가 직접 덮는다
- **거짓 통과 1건 발견·제거:** `test_demo_case_out_of_range_404`가 라우트 제거 후에도 통과했다. `/demo/999`가 404인 이유가 「범위 밖」이 아니라 「경로 없음」으로 바뀌었는데 단언은 그대로였다. **검사하던 것을 더 이상 검사하지 않는 테스트**라 지웠다
- `test_command`를 `python3 -m pytest -q` → **`pytest`**로 교정. 이 환경의 `python3`는 3.14로 해석되어 프로젝트 의존성이 없다. CI가 쓰는 명령과 일치시켰고 옵션은 `pyproject.toml`에 있다

**게이트 `A5`(버전 접두사 불일치) — 의도된 비대칭, 사유 기록.** `/api/v1`은 **소비자 계약**이라 버저닝하고 `/health/*`는 오케스트레이터가 부르는 **운영 계약**이다. 프로브 URL이 API 버전을 따라 움직이면 버전을 올릴 때마다 배포 설정이 깨진다. 대상이 다르므로 축도 다르다. 사유는 `SPEC.yaml`에 남겼다.

**커버리지 부채(항목별로 상환 예정)**

| 잃은 검증 | 상환할 항목 |
|---|---|
| 자연어 파싱 HTTP 경로 | `POST /api/v1/parsing-preview` |
| 데모 케이스 목록·단건 조회 | `GET /api/v1/demo-cases` |
| 필수필드 누락 → 422, 키 부재 → 503 | `POST /api/v1/assessments` 및 설명 실행 항목 |

### 항목 2 — `GET /health/ready` + 영속화 계층 (완료, 2026-08-28)

**항목 1의 관례를 복제했다.** 기능 단위 파일 분리, 태그 없는 원인→과정→결론 주석,
수용 기준을 명세·데이터 모델에서 테스트로 먼저 옮기고 실패를 확인한 뒤 구현.

| 만든 것 | 파일 | 근거 |
|---|---|---|
| 6개 모델 매핑 | `loan_agent/db/models.py` | 데이터 모델 §2 ERD. `status`/`verdict`는 `TEXT`+`CHECK`(ADR-021) |
| 엔진·세션·자원 상한 | `loan_agent/db/engine.py` | 풀 5+5·타임아웃 계층·`statement_timeout` 5초(ADR-022). import 시점이 아니라 첫 사용 시 생성 |
| readiness 판정 | `loan_agent/db/readiness.py` | DB 연결 + Alembic head 대조. LLM 제공자는 확인하지 않음 |
| `GET /health/ready` | `loan_agent/api/health.py` | 준비 안 됨 → 503 problem+json |
| 마이그레이션 1 (정합성) | `alembic/versions/0001_integrity.py` | PK·FK·UNIQUE·CHECK·부분 UNIQUE 2종. 성능 인덱스 제외(ADR-014) |
| 마이그레이션 2 (성능) | `alembic/versions/0002_performance_indexes.py` | 커서·상태필터·설명이력·감사추적 인덱스. §18 전후 비교용으로 분리 |
| 테스트 픽스처 | `tests/conftest.py` | ADR-008 — 실제 PostgreSQL. 세션당 마이그레이션 1회, 테스트당 트랜잭션 롤백 |

**순환 FK 처리.** `assessment_case` → `explanation_run` 두 테이블을 만든 뒤
`fk_case_current_run`을 따로 붙였다. 포인터가 nullable이라 런타임에서 실제 순환은 없다(§3).

**테스트 증거**

```
pytest tests/test_persistence.py tests/test_health.py   →  collection ImportError  (구현 전)
DATABASE_URL=… pytest tests/test_persistence.py tests/test_health.py   →  18 passed  (구현 후)
DATABASE_URL=… pytest --deselect test_eval.py::test_recorded_fixtures_full_pass   →  60 passed
alembic upgrade head / downgrade base / upgrade head   →  정상
```

**게이트:** 반복 구간이므로 돌리지 않았다(첫 항목에서 S4·L2·C3 확인 완료). 체크포인트에서 전체 실행.

**미결 — 내 항목 아님.** `tests/test_eval.py::test_recorded_fixtures_full_pass`가
`n == 15`를 기대하나 픽스처는 5건이라 실패한다. Codex Track B3(Eval 확대)의
진행 중 작업이 작업 트리에 미커밋 상태로 들어와 있다(`ci.yml`·`Dockerfile`·
`docker-compose.yml`도 동일). Track A는 이 파일을 건드리지 않는다. 스태시로
내 변경을 빼면 이 테스트는 통과 → 내 변경이 원인이 아님을 확인했다.

**발견 사항 — 미조치, `/verify`로 넘긴다.** `loan_agent/core.py:22`에
`[포트폴리오 정리]` 내부 태그가 남아 있다. 항목 2 범위 밖(core.py는 이 항목에서
수정하지 않음)이라 이번에 건드리지 않는다. 게이트 `L2`가 잡을지 체크포인트에서 확인.

### 항목 3 — `POST /api/v1/assessments` (R1/R2, 구현 완료 · 응답 형상 미결, 2026-08-28)

**항목 1의 관례를 복제했다.** 데코레이터에 전체 경로(`/api/v1/assessments`)를 쓰고
`include_router`에 prefix를 두지 않는다 — health와 같은 방식이고, 게이트의 경로 추출
(`PY_ROUTE`)이 prefix를 보지 않으므로 이렇게 해야 S3/S4 대조가 맞는다.

| 만든 것 | 파일 | 근거 |
|---|---|---|
| 결정적 판정 → 저장 형태 변환 + 버전 2종 | `loan_agent/decision.py` | ADR-005. `rule_version`은 손으로 올리는 문자열, `product_dataset_version`은 CSV 내용 해시라 파일이 바뀌면 저절로 달라짐 |
| 엔드포인트 · 요청 스키마 · 멱등성 · 트랜잭션 | `loan_agent/api/assessments.py` | ADR-003·004·019 |
| API가 실제 커밋하는 테스트 픽스처 | `tests/conftest.py::api_db` | `db_session`(롤백)으로는 병렬·커밋 확인 불가 |

**단일 트랜잭션.** `assessment_case(SCREENED)` + `decision_result` + `recommendation`×N
+ `explanation_run(PENDING)` + `audit_event`를 `sessionmaker.begin()` 한 블록에 쓰고
한 번 커밋한다. `explanation_run(PENDING)`을 같은 트랜잭션에 넣은 것이 아웃박스이고,
**LLM은 이 경로에서 호출하지 않는다** — 테스트가 `core.get_crew`/`get_llm`을 폭탄으로
바꿔 두고도 201이 나오는 것으로 고정.

**멱등성 3겹.** 선조회(같은 키·같은 해시 → 200 / 다른 해시 → 409) → INSERT →
`uq_assessment_idempotency_key` 위반 포착 → 재조회. 선조회는 최적화이고 최종 방어선은
UNIQUE다(ADR-004).

**테스트 증거**

```
python3.11 -m pytest tests/test_assessments.py   →  8 failed (404, 엔드포인트 없음)   구현 전
python3.11 -m pytest tests/test_assessments.py   →  8 passed                          구현 후
python3.11 -m pytest                             →  70 passed  (기준선 62 + 8)
```

T1 #9(병렬 같은 키 N=8 → 심사 정확히 1건, `explanation_run`도 1건) 포함.

**게이트:** 반복 구간이므로 돌리지 않았다(항목 1에서 S4·L2·C3 확인 완료). 체크포인트에서 전체 실행.
이 항목으로 `S3`는 7 → 6으로 준다(GET /assessments, GET /assessments/{id}, POST·GET
explanation-runs, POST /parsing-preview, GET /demo-cases 남음).

**미결 — 닫히지 않은 채 `/verify`가 잡았다(대장 #22).** `_serialize`는 진행 중
(PENDING/RUNNING) 설명 실행만 응답에 싣고, 재시도 항목(R6)에서 응답 형상을 확정하기로
했으나 항목 6에 그 확정 기록이 없다. 그 사이 GET은 `decision` 중첩 + `explanation_runs`
전체라는 다른 형상으로 굳었다. **사유 없는 형제 비대칭은 설계 검토 §1 기준 결함이며,
`/design`에서 두 응답 형상을 함께 확정한다.**

### 항목 4 — `GET /api/v1/assessments/{id}` (R3, 완료, 2026-08-28)

심사 상태·불변 판정·추천·설명 실행 이력을 한 응답에 결합했다. 판정에는
`rule_version`과 `product_dataset_version`을, 실행에는 모델·프롬프트·지연·토큰·오류·Eval
결과를 넣는다. 존재하지 않는 심사는 404 `problem+json`으로 응답한다.

### 항목 5 — `GET /api/v1/assessments` (R4, 완료, 2026-08-28)

상태 allow-list와 기간 필터만 받고, `(created_at, id)`를 base64로 감싼 불투명 커서를
쓴다. 정렬·필터의 컬럼 이름을 요청 값으로 만들지 않으므로 SQL 조립 경로가 없다. 기본
`limit`은 20, 상한은 100이며, 결과가 없을 때도 200과 빈 `items` 배열을 반환한다.

### 항목 6 — `POST /api/v1/assessments/{id}/explanation-runs` (R6, 완료, 2026-08-28)

설명 재생성은 판정 테이블을 읽거나 다시 쓰지 않고 새 `PENDING` 실행만 만든다. 기존
`PENDING` 또는 `RUNNING` 실행은 앱 검사와 `uq_explanation_run_in_flight`가 함께 막아
409로 응답한다. 실제 LLM 호출과 상태 전이는 별도 워커의 책임으로 남겼다.

### 항목 7 — `GET /api/v1/assessments/{id}/explanation-runs` (R7, 완료, 2026-08-28)

모든 실행의 모델·프롬프트 버전·지연·토큰·오류 코드와 Eval 결과를 반환한다. 자연어 원문과
전체 프롬프트는 컬럼 자체가 없어 조회 경로에도 실릴 수 없다.

### 항목 8 — `POST /api/v1/parsing-preview` (R5, 완료, 2026-08-28)

`core.rule_based_parse`와 `core.missing_required_fields`를 재사용해 구조화 후보와 누락
목록만 반환한다. DB 세션을 열지 않으며, 테스트가 호출 전후 `assessment_case` 행 수가 같은지
확인해 원문·후보가 심사 레코드로 남지 않음을 고정한다.

### 항목 9 — `GET /api/v1/demo-cases` (R9, 완료, 2026-08-28)

`core.load_demo_fixtures`의 사전 녹화 출력을 그대로 반환한다. API 키나 LLM 호출을 요구하지
않으므로 열람은 토큰 비용 0이다.

**테스트 증거**

```
pytest tests/test_assessment_followups.py  →  7 failed (404/405, 구현 전)
pytest tests/test_assessment_followups.py  →  8 passed               (구현 후)
pytest                                     →  78 passed, 1 warning
```

테스트 첫 실행 뒤 fixture 정리가 멈췄다. 원인은 `Engine.connect()`로 얻은 두 연결을 닫지
않아 `idle in transaction`이 된 것이고, `TRUNCATE`와 다음 마이그레이션이 그 잠금을 기다렸다.
테스트의 조회 연결을 context manager로 닫도록 바꾼 뒤 같은 focused 테스트와 전체 테스트가
끝까지 통과했다.

이 항목들로 `S3`의 남은 6건은 모두 구현됐다. 반복 구간 규칙에 따라 게이트는 실행하지
않았고, 체크포인트에서 한 번만 전체 판정했다.

**체크포인트 검증**

```
pytest                        →  78 passed, 1 warning
python3.11 tools/test_gate.py →  91/91 통과
python3.11 tools/gate.py .    →  BLOCK 1 · WARN 9
```

`S3`와 `S4`는 0건이다. 남은 BLOCK은 과거 커밋 이력 `G3` 하나이며, 이력 재작성 없이
해소할 수 없다는 기존 판단을 바꾸지 않는다. 기준선의 WARN 10건에서 9건으로 줄었고 새
BLOCK·WARN은 없다.

### Track C — 실행 경로·측정 증거 (측정 완료, C4는 Docker 부재로 미실행, 2026-08-28)

**C1.** Streamlit의 확정 심사는 `API_BASE_URL`의 `POST /api/v1/assessments`를 호출하고,
구조화 폼을 정규화한 UUIDv5를 `Idempotency-Key`로 보낸다. 같은 확인 입력은 같은 키를 쓰므로
중복 클릭도 서버의 멱등성 보장을 탄다. API 오류는 화면에 고정 문구만 보이고 오류 원문은 노출하지
않는다. 녹화 데모와 규칙 기반 폼 채우기는 키·API·DB 없이 동작해야 하므로 `core` 직접 호출을
유지한다. 데모는 녹화 파일을 읽고, 폼 채우기는 아직 저장하지 않는 후보만 만들기 때문이다.

**C1 — 인덱스 전후 실측.** Homebrew PostgreSQL 17.10, macOS 26.5.2 arm64, 논리 CPU 10개에서
합성 `assessment_case` 10,000건을 고정 UUIDv5·고정 시각·4개 상태 순환으로 생성했다. 그중
`SCREENED`는 2,500건이다. 측정 쿼리는 `status = 'SCREENED'` 필터, `created_at DESC, id DESC`
정렬, `LIMIT 100`이다.

| 상태 | 실행계획 | 실행시간 |
|---|---|---:|
| `0002` 적용 전 | `Seq Scan` 10,000건 → `Sort` | 0.771 ms |
| `0002` 적용 후 기본 플래너 | `Index Scan` · `ix_assessment_case_cursor`, 필터 후 299건 제거 | 0.048 ms |
| `0002` 후 커서 인덱스를 일시 제거 | `Index Only Scan` · `ix_assessment_case_status_cursor`, `Index Cond`로 상태 적용 | 0.042 ms |
| `0002` 후 상태 인덱스를 일시 제거 | `Index Scan` · `ix_assessment_case_cursor`, 필터 후 299건 제거 | 0.036 ms |

일반 플랜에서는 정렬을 보존하는 인덱스 스캔으로 바뀌고 실행시간이 줄었다. 다만 이 데이터
분포와 `LIMIT 100`에서는 플래너가 상태 선행 복합 인덱스보다 `(created_at, id)` 인덱스를
선택했다. 상태 선행 인덱스만 남긴 플랜은 `Index Cond`와 정렬 보존을 보여 컬럼 순서의
구조적 근거는 확인했지만, 실행시간은 커서 인덱스 단독 플랜보다 0.006ms 느렸다. 따라서
「등호 조건을 앞에 둔 인덱스가 항상 더 빠르다」고 일반화할 수 없고, 이 실험은 기존
컬럼 순서의 설계 가능성을 보인 정도다. 마이그레이션 파일은 합치지 않았고, 측정 종료 후
head로 되돌렸다.

**C2 — 결정적 경로 부하.** 같은 PostgreSQL 17.10 테스트 DB를 base에서 head로 초기화한 뒤,
단일 Uvicorn 프로세스의 `POST /api/v1/assessments`만 측정했다. 요청 본문은 고정된 유효
구조화 입력이고 모든 요청은 새 UUIDv4 `Idempotency-Key`를 사용했다. 서버는 macOS 26.5.2
arm64, 논리 CPU 10개, 앱 풀 `pool_size=5`, `max_overflow=5`, `pool_timeout=5초`,
`statement_timeout=5초`였다.

| 동시성 | 요청 수 | 처리량(req/s) | p50 | p95 | p99 | 오류율 |
|---:|---:|---:|---:|---:|---:|---:|
| 10 | 32,877 | 547.865 | 17.67 ms | 23.33 ms | 31.74 ms | 0% |
| 50 | 31,185 | 519.124 | 88.98 ms | 177.13 ms | 232.68 ms | 0% |
| 100 | 31,717 | 527.323 | 181.63 ms | 272.13 ms | 325.78 ms | 0% |

각 구간은 60초였다. DB는 본 측정 전에 빈 head 상태로 초기화했다. 출력 단계에서 실패한
초기 2초 smoke 시도도 요청 918건을 남겼고, 수정 후 smoke 2초 동시성 2에서 964건을
확인한 뒤 본 측정을 시작했다. 본 측정 3구간의 합계는 95,779건이며, 비용 조회 시점의
DB 누적은 97,661건이었다. 동시성 50부터 지연이 크게 늘고 처리량은 약 519~547 req/s에서
평탄해졌지만, 커넥션 풀 대기·타임아웃 오류는 관찰되지 않았다. 이 조건에서는 풀이 먼저
고갈됐다는 증거가 없고, 트랜잭션 밖에 LLM을 둔 설계가 커넥션 점유를 짧게 만든다는 방향은
지지된다. 다만 풀 점유율·CPU 프로파일을 측정하지 않았으므로 포화 원인을 특정하거나
ADR-022의 필요 커넥션 1개를 정확히 증명하지는 않는다. 절대값은 이 실행 환경과 데이터
건수에 한정한다.

**C3 — 토큰 비용.** 비용 상수는 `loan_agent/app.py`와 `tests/load/measure_track_c.py`의
`TOKEN_INPUT_USD_PER_MILLION`·`TOKEN_OUTPUT_USD_PER_MILLION`으로 분리했다. 2026-08-28
공식 모델 문서 기준 `gpt-4o-mini` 입력은 1M 토큰당 $0.15, 출력은 $0.60이다. 캐시 입력
할인은 모델링하지 않았다. 출처: [OpenAI 공식 모델 문서](https://developers.openai.com/api/docs/models/gpt-4o-mini).

측정 당시 DB에는 `explanation_run` 97,661건이 있었지만 입력·출력 토큰이 모두 기록된 행은
0건이었다. 따라서 운영 평균을 단정하지 않는다. 대신 2026-07-27 생성 사전 녹화 5개에
남은 누적 `usage_metrics` 스냅샷의 회차별 차분을 계산했다. 입력 평균 27,653.4토큰,
출력 평균 4,762.2토큰, 요청당 추정 비용 $0.00700533, 월 10,000건 추정 $70.0533이다.
이 관측치의 `successful_requests` 차분은 파이프라인마다 15건이었다. 세 Agent 단계가
곧 세 번의 제공자 호출이라는 뜻은 아니며, ReAct 도구 재조회와 프레임워크 호출이 포함된
실제 파이프라인 비용을 합산한 값으로만 해석한다. 라이브 결과 화면과 README에 토큰·요청당
비용·단가 기준일을 노출했다.

**C4 — 컨테이너 런타임.** Docker CLI 29.6.1의 `desktop-linux` 컨텍스트는 확인됐지만,
`docker info`가 `Cannot connect to the Docker daemon at unix:///Users/gwangsik/.docker/run/docker.sock`
로 실패했다. `docker compose config`에서는 서비스별 `cpus: 0.5`, `memory: 536870912`와
frontend/backend 네트워크를 확인했지만 이는 런타임 강제 증거가 아니다. 그러므로 컨테이너
inspect, 화면 제출의 `ui → app → postgres` 실제 경로, 워커의 상태 전이를 실행하지 않았고,
리소스 상한 채택 여부도 확정하지 않는다.

**C6.** README에 합성 데이터, 단순화된 DSR, Eval 커버리지, 실시간 제공자 장애 미검증을 요약하고
리스크 대장으로 연결했다. 통과율만으로 안전성이나 실서비스 적합성을 주장하지 않는다.

## 테스트 결과 · 증거 (출처 포함)
- C1 인덱스 측정: `tests/load/measure_track_c.py indexes --rows 10000`
- C2 부하 측정: `tests/load/measure_api_load.py --duration 60 --concurrency 10 50 100`
- C3 비용 측정: `tests/load/measure_track_c.py cost --monthly-requests 10000`
- C4 런타임 확인: `docker info`, `docker compose config`


### 압축 (v13 → v14, 2026-08-29) — `/compact`로 상태에서 내린 서술

**셀 상태는 하나도 제거하지 않았다.** 규제 5건·금지행위 9건·데이터 등급 4구분·준거 표준·승인 4행·유효 설계 결정 16항·미해결 라벨·미조치 피드백·정지 사유·다음 작업·기계 상태가 모두 `PROJECT_STATE.md`에 남아 있다. 아래는 **근거 서술**만 내린 것이며, 원문은 각 ADR에 그대로 있다.

| 내린 서술 | 원본 위치 |
|---|---|
| ADR-032의 측정 수치 — DSR 밴드 경계에서 `float`와 40자리 `Decimal`이 판정을 일치시키는 이유. **1원 입력 격자가 DSR을 `1e-9`씩 움직이는 반면 binary64 상대오차는 `1e-16`이라, 판정을 뒤집을 입력이 1원 격자 위에 존재하지 않는다.** 오차가 판정에 닿기 전에 입력 해상도가 먼저 끝난다 | `docs/설계결정.md` ADR-032 (측정표 3행 포함) |
| ADR-024 §24-R의 개정 사유 전문 — 방문자 키가 `ui` 세션 메모리에만 존재하고 워커는 별도 프로세스라 DB를 거쳐야 아는데 키 영속화가 금지 ⑦라는 논증 | ADR-024 §24-R |
| ADR-029의 「경계 3개 중 2개가 비었다」 진단과 기각 대안 4건 | ADR-029 |
| ADR-030의 왕복 5회 분해표와 「중복 심사」 논증 | ADR-030 |
| ADR-031의 AWS 채택 근거 3행·EKS 기각 논증·TLS 필수 논증 | ADR-031 §31.1·§31.2·§31.5 |
| 전자금융거래법 정의 4요소의 개별 대조 | `docs/리스크_통제_대장.md` A7 |
| ADR-033의 표준 판정표(IEEE 830→29148 등 대체 관계) | ADR-033 |
| 완료 판정 근거 3건의 상세 | 이 파일 「검증 결과」 절 |

**압축 후:** `PROJECT_STATE.md` 101줄 → **91줄.** `/build` 중 증가분을 흡수할 여유를 남겼다.

### 세션 인계 시점 압축 (v8 → v9, 2026-08-28)

`PROJECT_STATE.md`에서 내린 것 — 삭제가 아니라 이동이다.

- **Track C 측정 요약 서술** — C1 인덱스 전후·C2 부하·C3 토큰 비용의 조건과 수치. 원문은 이 파일의 「Track C — 실행 경로·측정 증거」 절에 있고, 상태 파일에는 「완료」와 「C4만 미실행」만 남겼다
- **`/build` 항목별 진행 서술** — 항목 1~9와 워커의 구현 순서·중간 게이트 결과. 이 파일의 「구현 상태」 절에 있다
- **피드백 #4(`I3`)·#5(`D2`)** — 둘 다 해소돼 대장에만 남기고 미조치 목록에서 뺐다
- **게이트 WARN 수치의 시점별 변동** — 재측정하면 나오는 값이라 남기지 않는다

압축하며 상태 파일에 **보강한** 것(다음 세션이 그것만 읽고도 실수하지 않도록):

- ADR-012에 **실행 상태와 심사 상태의 어휘가 다르다**는 사실을 명시. 구현 중 이 둘을 혼동해 DB CHECK에 걸렸고, 코드와 테스트가 같은 오해를 담고 있어 앱 검증만으로는 드러나지 않았다
- ADR-004/019에 **그 사건이 이중 방어의 실사례**임을 한 줄로 남김
- 데이터모델 항목에 **인덱스 마이그레이션 두 개를 합치지 말 것**을 명시 — 합치면 전후 측정이 성립하지 않는다
- `G3`가 **원리상 해소 불가**임을 명시. 이전 서술은 「문서 개편에서 해소된다」였는데 성립하지 않는다
- ADR-020(로컬 sLLM 비교축)에 **미착수**임을 명시 — 유효한 결정 목록에만 있으면 완료된 것으로 오독된다

## 검증 결과 (`/verify`)
> 항목별 판정·근거 전문. `PROJECT_STATE.md`에는 판정값만 남긴다.

```text
pytest
89 passed, 1 warning in 2.33s

python3.11 tools/test_gate.py
91/91 통과

python3.11 tools/gate.py .
╰─ BLOCK 1 · WARN 9
```

기준선에 적힌 `pytest 88 passed` 대비 테스트는 비용 계산 회귀 테스트 1건이 늘었다.
기준선에 적힌 `gate BLOCK 1 · WARN 8`과 달리 현재 HEAD를 변경 없이 임시 복제해 같은
게이트를 실행해도 `BLOCK 1 · WARN 9`가 나왔다. 따라서 이번 변경으로 게이트 경고가
증가한 것은 아니며, 기준선 기록과 저장소 실제 HEAD 출력 사이에 WARN 1건의 불일치가
있다. BLOCK은 기존 커밋 이력 `G3` 하나이고, 새 표면 누락·과잉(`S3`·`S4`)은 0건이다.

### Codex 이행 결과 검증 (2026-08-29, 본인 · 격리 컨텍스트 아님)

`prompts/codex-build-approved-design.md`를 Codex가 실행한 결과를 대조했다. **커밋하지 않았고, 지시를 어긴 것도 없다.**

**1차 — 요청한 것이 동작하는가: 전 항목 이행**

| 항목 | 판정 | 확인 방법 |
|---|---|---|
| `#21` 태그 19건 | 고침 | `grep` 잔존 0건. 설명 문장 보존 |
| `#15` healthcheck | 고침 | `ui`·`app`·`worker` 3개 추가. **`Dockerfile:15`에 `curl`이 실제로 설치되는지 확인** — 없었으면 unhealthy가 그대로 남는다 |
| `#14` CI | 고침 | 개별 패키지 나열 → `requirements.txt` 전체 설치 |
| `#19` | **손대지 않음(지시 준수)** | diff의 토큰 추출식은 context 라인이었다 |
| `#26` | 고침 | 단언 + 테스트 이름까지 반전 |
| `#29` | 고침 | `Mapped[Decimal]` + `Numeric(18,12)` + 마이그레이션 `0003` + 왕복 타입 통합 테스트 |
| ADR-029 | 이행 | 두 파서 병행 · 자동 화해 없음 · `degraded` 플래그 · LLM 1회 · `#18` Eval 배선 |
| ADR-030 | 이행 | `generate_guidance` 1회 호출, 도구 왕복 4회 제거 |
| ADR-024 §24-R | 이행 | 키 헤더 · 기존 `PENDING` 집기 · 스테일 `RUNNING` 회수 · `RUNNING` 중 409 · 상한 초과 503 |

**직접 대조한 불변 조건.** 금지행위 ⑦ — 키가 DB·로그·`os.environ` 어디에도 없다. ADR-003 — `claim_for_visitor`가 커밋으로 끝나고 LLM은 그 밖에서 돈다. ADR-007 — Eval 미통과분은 **저장 자체를 하지 않으므로** 새로 노출한 `explanation_text`가 NULL이다. 인덱스 마이그레이션 2개 병합 없음. `endpoints:` 불변.

기계: `pytest 89 → 99 passed` · `gate BLOCK 1(G3) · WARN 9`(변동 없음) · `S3`·`S4` 0건.

**2차 — 변경 후 시스템이 일관한가: 결함 4건(`#33`~`#36`)**

`#33` 죽은 코드(게이트 `S2` 8→9) · `#34` 노트북이 폐기된 파이프라인 시연 · `#35` 명세에 없는 계약 · `#36` 스키마 변경분 문서 미반영. 전문은 피드백 대장.

**보고 누락 1건.** 프롬프트가 요구한 **ADR-030 단계별 실측**이 없다. 게다가 측정 수단(`run_service_with_stages`)이 `#33`으로 죽어 **지금은 잴 방법도 없다.** 1회 호출 전체 시간으로 대체해야 한다.

**`#35`의 원인은 인계 프롬프트에 있다.** 「`endpoints:` 수정 금지」라고만 써서 Codex가 문자 그대로 지켰는데, `SPEC.yaml`은 헤더·응답 형상·오류 코드도 주석으로 관리하는 파일이다. **다음 인계 프롬프트에서 이 문구를 고친다.**

### 독립 검증 1회 (2026-08-28, `independent-verifier` 격리 컨텍스트)

**판정: 배포 보류.** 지적 13건(대장 #13~#25) 전부 결함이며, 제안 4건은 별도로 아래에 둔다.

검증자는 측정 스크립트를 재실행해 증거를 대조했다. **C1 인덱스·C3 토큰 비용은 재현
성공** — `measure_track_c.py cost --monthly-requests 10000`이 로그의 `0.00700533`/`70.0533`과
정확히 일치했고, 인덱스 실행시간·플랜(`rows_removed_by_filter 299`)도 표와 같았다.
**C2 부하만 원시 출력이 저장소에 없다** — 스크립트는 실재하나 95,779건 측정의 JSON·로그가
남아 있지 않아 서술 수치만 있다. C1·C3와 증거 등급이 다르므로 「부분 증거」로 기록한다.

통과한 축: 근거·환각(라벨 잔존 0건), **규제 서술**(「준수했다」로 읽히는 문장 없음 —
「조건 밖으로 설계했다」 프레임 유지), 금지 자동화 행위(흔적 없음, 최근 25커밋 author 전부
noreply, git 이력의 `sk-proj-` 매치는 노트북 자리표시자), 관례 대비 검토(SDK 내장 재시도·
`FOR UPDATE SKIP LOCKED`·커서 페이지네이션 모두 부합), 명세 대칭(`S3`·`S4` 0건).

미충족 축: 완료 기준(문서-구현 일치, 실행·재현·복구 절차), 상태 정합성, 주장 대 실제,
설계 정합성. **결함이 몰린 자리는 이번에도 경계였다** — 문서와 코드 사이(#13), 트랙과 트랙
사이(#14 CI, #15 컨테이너), 화면과 API 사이(#16·#17), 어휘 사이(#23 멱등 키의 의미).

**보고를 그대로 받지 않고 13건 전부 직접 재확인했으며, 한 건은 보고보다 심각했다.**
#21의 내부 태그는 2건이 아니라 15건이다(`[피드백①~④]` 13건이 `core.py`에 남아 있다).
대장 #1이 `[디벨롭:` 문자열만 지우고 다른 태그 형태를 놓친 결과이며, 게이트 `L2`가
이 형태를 잡지 못한다는 사실도 함께 발견 사항이다.

상태 파일 불일치 2건도 이번에 드러났다 — `PROJECT_STATE.md`의 「Build complete」와
`PROJECT_LOG.md` 항목 3의 「진행 중」이 어긋나고, 「Open Labels: (none)」은 같은 파일이
`/verify`로 넘긴 내부 태그 잔존과 모순된다. 아래 「구현 상태」와 상태 파일에서 교정한다.

**제안 (결함 아님, 채택 여부는 열려 있다)**
- C2 부하 측정의 원시 출력(JSON·로그)을 저장소에 남기면 C1·C3와 같은 증거 등급이 된다
- `db/models.py:43-45`의 CHECK 제약 f-string 조립은 DDL 특성상 불가피하나 설계 검토 §6이 요구하는 사유 한 줄이 없다
- 대장 #1·#4의 커밋 열이 「미커밋」으로 낡았다 — 작업 트리는 clean이다
- `README.md:227`의 DSR 40% 근거에 1차 출처가 없다(같은 절의 다른 수치에는 있다)

**되돌아갈 단계.** `/design` → #16·#17·#20·#22·#23·#24 (판단이 필요한 것).
`/build` → #13·#14·#15·#21 (판단이 끝난 것). `/debug` 경유 → #18·#19·#25
(재현 테스트를 먼저 실패시킨 뒤 고친다 — 특히 #18은 현재 테스트가 두 함수를 전부
대역으로 바꿔 놓아 회귀를 잡지 못한다).


## 뒤집힌 설계 결정
> `PROJECT_STATE.md`에서 내려온 결정. **무효화한 부속 규격과 그 처리 결과를 함께 적는다.**

| 결정 | 뒤집은 이유 | 무효화한 부속 규격 | 처리 결과 |
|---|---|---|---|
| **ADR-024 「LLM을 부르는 주체는 워커뿐이다」** (2026-08-28 → 2026-08-29 부분 폐기) | 확정된 사용자 요구 「방문자가 자기 토큰으로 시현할 수 있어야 한다」와 양립 불가. 방문자 키는 `ui` 세션 메모리에만 있고 워커는 별도 프로세스라 DB를 거쳐야 아는데, **키를 DB에 넣는 것은 금지 자동화 행위 ⑦**이다 | ①ADR-024 기각 대안 표 첫 줄(「실행 경로가 둘로 갈린다」)의 판단 ②`SPEC.yaml` 비HTTP 표면 절의 「Sole executor」 문단 | **ADR-024 §24-R로 개정**(폐기가 아니라 조건 분기 — 키 출처에 따라 워커/`app`). `SPEC.yaml` 해당 문단 재작성. **개정 전 우려였던 「감사 경로 두 벌」은 발생하지 않는다** — 두 경로가 같은 `explanation_run` 행을 집어 같은 컬럼을 채운다. 원래 결함(행 없는 실행)은 그대로 해소된다 |
| **ADR-024 「검토하지 않은 전제」 유보 조항** | 공개 데모 처리가 ADR-031로 확정돼 유보 사유가 소멸 | 없음 | ADR-031에서 배포 형상 확정. 유보 해제 |

## 명세 개정 이력
> `/design` §5로 `SPEC.yaml`을 고칠 때마다 한 줄 추가한다.
> **이 표가 길어지면 설계 단계가 얕았다는 신호다.**

| 추가·삭제 항목 | 구분 | 사유(요구사항 지목) | 승인 | 시각 |
|---|---|---|---|---|
| 설명 실행기(비HTTP 표면) | **설계 누락 보완** | **R7** — 설명 시도가 모델·프롬프트 버전과 함께 조회 가능해야 하는데, `PENDING`을 집어 실행하고 결과를 기록하는 주체가 저장소에 없었다. `model_name`·`prompt_version`·토큰·`explanation_text`가 영구히 비고, `eval_result`에 행이 생기지 않으며, 상태가 `SCREENED`에서 멈춰 **네 상태 중 셋이 도달 불가능**했다. ADR-003이 「실제 호출은 별도 실행기의 몫」이라 적고 T1 #7이 그것을 아웃박스라 불렀으므로 **범위 확장이 아니라 이미 한 설계의 이행**이다 | 사용자 승인(2026-08-28) | 2026-08-28 |
| 오류 정책 429 문구: 「세션당」 → 클라이언트 주소 기준, API가 강제 | **설계 누락 보완** | **명세 자체가 구현 불가능한 문구였다.** 인증 표면이 없으므로(ADR-009) API에 세션 개념이 없다. 그래서 상한이 Streamlit 카운터에만 남았고, 화면이 API를 호출하는 구조가 된 뒤 API 표면에는 상한이 사라졌다. 413·415는 문구 변경 없이 구현만 추가한다(ADR-028) | 사용자 승인(2026-08-28) | 2026-08-28 |
| 심사 응답 형상 통일 — POST를 GET 형상(`decision` 중첩 + 배열)으로 | **설계 누락 보완** | **R1/R3.** 같은 자원의 형제 응답이 형상이 달라 클라이언트가 동사에 따라 분기해야 했다. 항목 3의 「재시도 항목에서 확정한다」는 미결이 항목 6에서 닫히지 않은 채 굳은 것이며, 사유 없는 형제 비대칭은 설계 검토 §1 기준 결함이다(ADR-026) | 사용자 승인(2026-08-28) | 2026-08-28 |
| 추천 항목 표현에 상품 상세(상품명·은행·금리범위·한도) 포함 | **설계 누락 보완** | **R1.** `product_code`·`rank`만으로는 화면이 추천을 렌더할 수 없어 UI가 `None`을 하드코딩했고 렌더 분기 전체가 도달 불가가 됐다. **엔드포인트를 추가하지 않으므로 철회된 R8의 재등재가 아니다** — 이미 있는 자원의 표현을 채우는 것이고, 시나리오는 실재한다(ADR-025) | 사용자 승인(2026-08-28) | 2026-08-28 |
| 설명 실행 주체를 워커로 단일화(화면 직접 호출 폐기, 기존 GET 폴링) | **설계 누락 보완** | **R7.** 화면이 파이프라인을 직접 돌려 방문자가 실제로 읽은 안내문에 `explanation_run` 행이 없었다 — 모든 설명 시도가 조회 가능해야 한다는 요구가 바로 그 시도에서 깨졌다. 같은 클릭이 만든 PENDING을 워커가 또 실행해 토큰이 두 배였다. **폴링은 R7이 이미 가진 GET을 읽으므로 새 표면이 없다**(ADR-024) | 사용자 승인(2026-08-28) | 2026-08-28 |
| R5 문구: 파서 1개 → **독립 파서 2개 + 불일치 노출** | **설계 누락 보완** | **R5 자체.** 명세는 「구조화 후보로 바꾼다」였고 구현은 규칙 파서만 썼다. LLM 파서(Agent 1)는 안내문 경로에 있었는데 **출력이 검증도 사용도 되지 않았고**, Eval 「파싱정확도」 지표는 채점 대상이 없었다(대장 #18). 두 파서의 불일치를 응답에 실어 **경계에 결정적 검증을 넣는다.** 저장하지 않는 성질은 그대로이고 **엔드포인트는 늘지 않는다**(ADR-029) | 승인 대기 | 2026-08-29 |
| 안내문 생성: LLM 왕복 5회 → **1회** (도구 왕복 4회 제거) | **설계 누락 보완** | **R6/R7.** 계약(응답 필드)은 바뀌지 않는다. 파이프라인 안의 심사가 `POST /assessments`가 이미 계산해 저장한 판정을 **다시 하고 버리는** 중복이었다. 확정 판정·추천 상세를 프롬프트에 데이터로 주입한다. **보장이 강해진다** — 「도구를 부르라고 시키는 것」에서 「생성할 대상이 없는 것」으로(ADR-030) | 승인 대기 | 2026-08-29 |
| 비HTTP 표면: 설명 실행 주체를 **키 출처에 따라 분기** | **설계 누락 보완** | **R6/R7.** ADR-024의 「워커 단일화」가 확정 요구와 충돌. 두 경로 모두 같은 행을 집고 같은 컬럼을 채우므로 감사 경로는 하나다. **새 엔드포인트 없음** — 기존 R6 POST에 키 헤더가 오면 동기 실행(ADR-024 §24-R) | 승인 대기 | 2026-08-29 |
| - | 설계 누락 / 범위 확장 | - | - | - |

## 인사이트 수집 (`/insight add`)
> 이 프로젝트에서 나온 **재사용 가능한 판단**만 적는다. 사건 서술이 아니라 규칙 문장으로 쓴다.
> 하네스 자산으로 승격할지는 `/insight promote`가 판정한다. 승격 전까지는 이 프로젝트의 것이다.

- (없음)

## 폐기한 접근과 이유
- (없음)

## 포트폴리오 자동화 상태 (셀 상태 — `/compact`에서 보존)
- **대화 입력:** (없음 / 요약 파일 경로)
- **증거 추출:** 미실행
- **evidence.yaml:** 미생성
- **project.yaml:** 미생성 (status_source: )
- **PROJECTS.md:** 미생성
- **사용자 검토 승인:** 미승인 (승인 시각: )
- **발행 기준 검증(portfolio-criteria):** 미실행
- **보고서 프롬프트(/handoff report):** 미생성 (실행 주체: / prose-auditor 판정: )
- **공개 초안 프롬프트(/handoff publish):** 미생성 (실행 주체: / internal·public 구분: )
- **민감정보 스캔:** 미실행
- **외부 전송:** 미실행 (승인 시각: )
