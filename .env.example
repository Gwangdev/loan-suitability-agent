# 이 파일을 복사해 `.env` 로 저장한 뒤 실제 값을 채우세요.
#   cp .env.example .env
#
# .env 는 .gitignore 로 커밋되지 않습니다(비밀키 보호).
# LLM 파이프라인(3-Agent)을 실행할 때만 키가 필요하며,
# 결정적 심사 로직/규칙기반 파서/자체 테스트는 키 없이 동작합니다.

OPENAI_API_KEY=sk-여기에_본인_키
OPENAI_MODEL_NAME=openai/gpt-4o-mini
