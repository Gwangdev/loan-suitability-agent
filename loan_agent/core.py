"""모든 계층이 함께 쓰는 상수.

심사 규칙·상품 데이터·파서·데모 자료는 각각 `screening`·`products`·`parser`·`demo`로 나뉘어 있다.
여기에는 그중 어느 하나에도 속하지 않고 계층을 가로질러 쓰이는 값만 둔다 — 경로 기준점, 안내문에
반드시 들어가는 고지 문구, 설명 실행 상한이다.
"""
from pathlib import Path

# 이 파일의 상위 폴더가 프로젝트 루트(CSV·.env 위치). cwd에 의존하지 않는다.
BASE_DIR = Path(__file__).resolve().parent.parent

# 교육용 디스클레이머(모든 안내문에 필수 포함 — 규제 통제)
DISCLAIMER = (
    "본 안내는 교육용 데모이며 실제 대출 심사·법적·금융 자문이 아닙니다. "
    "실제 대출은 각 금융기관 심사를 따릅니다."
)

# ADR-022가 정한 설명 실행 상한의 단일 정의다. 동기 요청·워커·화면이 같은 실행을
# 서로 다른 값으로 끊으면 상태 전이와 사용자 경험이 갈라진다.
EXPLANATION_RUN_TIMEOUT_SECONDS = 200
