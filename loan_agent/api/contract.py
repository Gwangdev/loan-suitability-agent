"""HTTP 계약에서 이름 자체가 계약인 값들.

라우터는 서로를 임포트하지 않고 여기만 본다. `api/__init__.py`에 두면 그 파일이
라우터를 임포트하고 라우터가 다시 그것을 임포트해 순환이 생긴다 — 계약 상수는
아무것도 임포트하지 않는 잎에 있어야 한다.
"""

# 방문자가 자기 LLM 키를 보내는 헤더. 두 라우터가 같은 값을 써야 하는데 한쪽에 오타가
# 나면 그 경로만 조용히 키를 못 받는다. 실패가 아니라 저하로 나타나므로 테스트를
# 통과한 채 배포될 수 있다. 그래서 문자열을 흩지 않고 한 곳에서만 정의한다.
API_KEY_HEADER = "X-OpenAI-API-Key"

__all__ = ["API_KEY_HEADER"]
